# Archive Index — 2026-06

| Feature | Match Rate | Archived | Documents |
|---------|-----------|----------|-----------|
| dashboard-test-flakiness | 100% | 2026-06-11 | Plan, Report (Design 생략 — 명확한 test mock 추가). PR #46(`fix/dashboard-test-flakiness`) admin squash 머지(`890d783`, base main). `dep-upgrade` PR #34 CI 발견·분석 **M7**·**Task #8**. `CustomerDashboard.test.tsx`의 CI 무작위 `check` 차단(`"window is not defined"` unhandled, 타이밍 flaky: 로컬 10/10·CI 재실행 green) **결정적 제거**. 근본원인: 페이지 렌더 테스트가 대시보드 훅 **8개 중 3개만** mock → 미mock 5개(`useFscRates`/`useMarginRules`/`useSurcharges`/`useAddonRates`/`useResolvedMargin`)가 실제 async fetch → jsdom teardown 후 콜백 resolve가 `window`-의존(authStorage/localStorage/Sentry)에 닿음 → vitest exit 1. 수정(test-side, 기능 코드 무변경): 나머지 5훅 `vi.mock`(기존 3-mock 패턴, 빈/기본 stub; `useSurcharges`는 `calculateApplied()=[]`/`totalAmount()=0`, `useFscRates`는 중첩 `rates` 기본값) → 8/8 mock·실제 async 0. 검증: CustomerDashboard 10/10(원base)+5/5(rebase) clean, 전체 vitest **56f/1464t** green, lint `--max-warnings 0`·build OK, **PR #46 `check` job green(재실행 없이)**+backend-rspec/e2e/lighthouse pass(SC-4). ⚠️ Reconcile: 작업 중 로컬 main이 origin보다 **10커밋 behind**(#36~#45, 처음 'ahead' 오판→fetch 후 정정)→clean FF→새 브랜치 재적용. 신규 `AdminWidgets.test`(#43)는 위젯 컴포넌트레벨 stub라 5훅 누수 무관 재확인. 교훈: 로컬 green≠CI green(jsdom teardown async)·페이지 렌더 테스트는 transitive 훅 전부 mock·측정 전 `git fetch`+behind 확인. 후속: `dashboard-hooks-abort-guard`(훅 unmount 가드, 선택). |
| dep-upgrade | 100% | 2026-06-07 | Plan, Report (Design 생략 — 의존성). PR #34(`chore/dep-upgrade`) admin squash 머지(`21c11b9`, base main). 코드분석 2026-06-07 **H1** 해소: prod 런타임 취약 의존성 업그레이드(앱 코드 무변경). react-router-dom 7.13.1→7.17.0(high turbo-stream RCE, v7 minor non-breaking) + jspdf 2.5.1→**4.2.1**(critical ReDoS, major) + jspdf-autotable 5.0.7→5.0.8(peer `^2\|\|^3\|\|^4` 호환=R-1 해소) + dompurify 2.5.8→3.4+(moderate XSS, transitive). 검증: **prod `npm audit` 0 vulnerabilities**, tsc/lint/vitest 1447/build green, **PDF real-jsPDF 테스트(pdfLayout+pdfService) 8 pass**로 jspdf major breaking 0. CI 전 job green(check 1회 flaky fail→재실행 green). ⚠️ Do 중 신규 발견(dep 무관): `useExchangeRates.ts:53,64,82` 폴링 setInterval/window listener async가 jsdom teardown 후 발현 → CustomerDashboard 테스트 "window is not defined" **flaky**(로컬 5/5·CI 재실행 green), main check 랜덤 차단 가능 → Task #8 + 분석 M7 `dashboard-test-flakiness` 후속. 잔여: 수동 PDF 육안(배포 후). 교훈: npm isSemVerMajor로 jspdf major 사전포착·실제-jsPDF 테스트 안전망·로컬≠CI 3번째. |
| backend-ci-gate | 100% | 2026-06-07 | Plan, Report (Design 생략 — CI 1줄). PR #32(`ci/backend-ci-gate`) admin squash 머지(`c6ffe4e`, base main). `backend-test-infra-debt` 후속 **R-3 완료**: `ci.yml` `backend-rspec` job을 auth 2개(`auth_spec`+`auth_mailer_spec`)만 실행 → 전체 `bundle exec rspec`(18파일/243 examples)로 확장해 FSC/margin 드리프트 재발 차단. ⚠️ **핵심 발견**: 확장 직후 CI **7 fail**(로컬 0) — `db:prepare`가 청정 CI test DB에 `margin_rules` 시드(`db/seeds/margin_rules.rb` 한국어 룰 14개)→`use_transactional_fixtures` 밖 커밋으로 `margin_rules_spec`(rules.length 2→16·resolve 명/마진)·`margin_rule_resolver_spec` 4건 오염. 로컬은 기존 prepared DB라 시드 스킵돼 미발현. 수정: `db:prepare`→**`db:test:prepare`**(스키마만, 시드 없음; factory+`maintain_test_schema!` 설계 정합, Postgres service가 DB 생성). 검증: backend-rspec CI **243/0**, **fail-injection**(`fsc_rate_spec` 47.25→99.99 → RED `fsc_rate_spec:48` 포착 → revert → GREEN)로 게이트가 새 커버 spec 실패 잡음 실증. 머지 net diff=ci.yml+plan+meta(injection/revert 상쇄). 기능 코드 무변경. 교훈: 로컬 green≠CI green(시드 차이) / 부분 게이트가 함정 은닉 / CI 확장 검증은 CI 실측 필수. → [[feedback_ci_seed_pollution_db_test_prepare]] |
| backend-test-infra-debt | 100% | 2026-06-07 | Plan, Report (Design/Analysis 생략 — 스펙 한정·Plan=spec). PR #30(`fix/backend-test-infra-debt`) admin squash 머지(`38d28bc`, base main). `/check`(2026-06-07)에서 발견된 `smart-quote-api` rspec pre-existing 실패를 **스펙 한정**(프로덕션 코드 무변경)으로 복구: **G1** `fsc_rate_spec` UPS/DHL 기대값 하드코딩(48.5/46.0)→`Constants::Rates::DEFAULT_FSC_PERCENT`/`_DHL`(50.25/48.75) 바인딩(FSC 갱신 시 drift 재발 차단; `have_attributes` 첫 불일치로 가려졌던 DHL stale 동반 해소), **G2** `margin_rule_spec`/`margin_rules_spec` 모델 `margin_percent>=0` 계약에 정렬(하한 5→0%·`subject{build}`로 shoulda `name` 간섭 제거·request invalid 3→-1; R-1 결정=0% 유지). ⚠️ **origin/main 재조정**: 분석을 9커밋 behind 로컬 main에서 수행했으나 origin은 #21/#24/#25/#29(magic_link·auth 하드닝)가 **G3(`/auth/me` 중첩)·G4(magic_link `deliveries.count`)를 이미 해결** → G3/G4 폐기, G1/G2만 origin 기반 재적용. 검증: origin 기반 전체 rspec **243/0**, 회귀 0, rubocop 변경분 0. CI 전 job green(check·backend-rspec·e2e·lighthouse). 교훈: 측정 전 `git fetch`+behind 확인 / `have_attributes` 첫 불일치 후속 점검 / 테스트를 source 상수 바인딩 / CI 부분 게이트(`backend-rspec` job이 auth 2개만 실행) 평가. 후속: **R-3 `backend-ci-gate`**(`ci.yml` `backend-rspec` line59를 전체 `bundle exec rspec`로 확장, 본 머지로 243/0이라 안전) 미시작. |
| chat-security-hardening | 100% | 2026-06-06 | Plan, Report (Design/Analysis 미작성 — 분석은 `docs/03-analysis/codebase-health-2026-06-06.analysis.md` 다중-finding 공유). PR #20(`fix/chat-security-hardening`) admin squash 머지(`ca82f69`). 코드베이스 분석(2026-06-06, health 6.7/10)에서 검증된 AI 챗봇·마진 보안 HIGH 4종 백엔드 하드닝: **H1** chat per-user rate limit 20/min(Rails.cache, IP 아닌 user 키로 NAT 회피) → 429, **H2** `margin_rules#resolve` 본인 email/admin만(타 email **403**, 가격 enumeration 차단; 프론트는 항상 `user?.email`만 전달해 회귀 0), **H3** 시스템 프롬프트 user name/company sanitize(개행/제어문자 제거·유니코드 보존; admin_guide는 이미 `is_admin` 코드 게이트라 권한상승 아닌 인젝션 위생 — 서브에이전트 "CRITICAL 권한상승" 과장 정정), **H6** Anthropic client `timeout: 30, max_retries: 0`(기본 600s×2≈30분 행 방지). TDD: resolve 403 RED→GREEN + chat 신규 spec, **신규 12 test GREEN**(resolve 6 + chat 6), 전체 rspec 235/7 실패는 전부 pre-existing(fsc seed·margin **model** validation·auth, 본 변경 무관), 회귀 0, rubocop 내 코드 clean, 프론트 무변경. CI 전 job green(check·e2e·lighthouse). Render prod 동작 검증은 인증 게이트(401만 관측 가능)+대시보드 접근 없음으로 수동 후속. advisor 검토 5건 반영(per-user 키/유니코드 보존 sanitize/max_retries 0/테스트 하니스 선스모크/H2 프론트 usage 확인). 후속 후보: 백엔드 rubocop 1054+rspec 7 정리, calc 오케스트레이터(emax 공통), VITE_SLACK_WEBHOOK_URL 점검. |
