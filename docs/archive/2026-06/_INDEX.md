# Archive Index — 2026-06

| Feature | Match Rate | Archived | Documents |
|---------|-----------|----------|-----------|
| backend-test-infra-debt | 100% | 2026-06-07 | Plan, Report (Design/Analysis 생략 — 스펙 한정·Plan=spec). PR #30(`fix/backend-test-infra-debt`) admin squash 머지(`38d28bc`, base main). `/check`(2026-06-07)에서 발견된 `smart-quote-api` rspec pre-existing 실패를 **스펙 한정**(프로덕션 코드 무변경)으로 복구: **G1** `fsc_rate_spec` UPS/DHL 기대값 하드코딩(48.5/46.0)→`Constants::Rates::DEFAULT_FSC_PERCENT`/`_DHL`(50.25/48.75) 바인딩(FSC 갱신 시 drift 재발 차단; `have_attributes` 첫 불일치로 가려졌던 DHL stale 동반 해소), **G2** `margin_rule_spec`/`margin_rules_spec` 모델 `margin_percent>=0` 계약에 정렬(하한 5→0%·`subject{build}`로 shoulda `name` 간섭 제거·request invalid 3→-1; R-1 결정=0% 유지). ⚠️ **origin/main 재조정**: 분석을 9커밋 behind 로컬 main에서 수행했으나 origin은 #21/#24/#25/#29(magic_link·auth 하드닝)가 **G3(`/auth/me` 중첩)·G4(magic_link `deliveries.count`)를 이미 해결** → G3/G4 폐기, G1/G2만 origin 기반 재적용. 검증: origin 기반 전체 rspec **243/0**, 회귀 0, rubocop 변경분 0. CI 전 job green(check·backend-rspec·e2e·lighthouse). 교훈: 측정 전 `git fetch`+behind 확인 / `have_attributes` 첫 불일치 후속 점검 / 테스트를 source 상수 바인딩 / CI 부분 게이트(`backend-rspec` job이 auth 2개만 실행) 평가. 후속: **R-3 `backend-ci-gate`**(`ci.yml` `backend-rspec` line59를 전체 `bundle exec rspec`로 확장, 본 머지로 243/0이라 안전) 미시작. |
| chat-security-hardening | 100% | 2026-06-06 | Plan, Report (Design/Analysis 미작성 — 분석은 `docs/03-analysis/codebase-health-2026-06-06.analysis.md` 다중-finding 공유). PR #20(`fix/chat-security-hardening`) admin squash 머지(`ca82f69`). 코드베이스 분석(2026-06-06, health 6.7/10)에서 검증된 AI 챗봇·마진 보안 HIGH 4종 백엔드 하드닝: **H1** chat per-user rate limit 20/min(Rails.cache, IP 아닌 user 키로 NAT 회피) → 429, **H2** `margin_rules#resolve` 본인 email/admin만(타 email **403**, 가격 enumeration 차단; 프론트는 항상 `user?.email`만 전달해 회귀 0), **H3** 시스템 프롬프트 user name/company sanitize(개행/제어문자 제거·유니코드 보존; admin_guide는 이미 `is_admin` 코드 게이트라 권한상승 아닌 인젝션 위생 — 서브에이전트 "CRITICAL 권한상승" 과장 정정), **H6** Anthropic client `timeout: 30, max_retries: 0`(기본 600s×2≈30분 행 방지). TDD: resolve 403 RED→GREEN + chat 신규 spec, **신규 12 test GREEN**(resolve 6 + chat 6), 전체 rspec 235/7 실패는 전부 pre-existing(fsc seed·margin **model** validation·auth, 본 변경 무관), 회귀 0, rubocop 내 코드 clean, 프론트 무변경. CI 전 job green(check·e2e·lighthouse). Render prod 동작 검증은 인증 게이트(401만 관측 가능)+대시보드 접근 없음으로 수동 후속. advisor 검토 5건 반영(per-user 키/유니코드 보존 sanitize/max_retries 0/테스트 하니스 선스모크/H2 프론트 usage 확인). 후속 후보: 백엔드 rubocop 1054+rspec 7 정리, calc 오케스트레이터(emax 공통), VITE_SLACK_WEBHOOK_URL 점검. |
