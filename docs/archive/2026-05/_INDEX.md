# Archive Index — 2026-05

| Feature | Match Rate | Archived | Documents |
|---------|-----------|----------|-----------|
| smart-quote-e2e-debt | 100% | 2026-05-02 | Plan, Design, Analysis, Report — PR #7(insights-scaffold) Check 단계에서 처음 노출된 e2e fail 9건 / 5 spec files 분리 사이클(`feedback_split_cycle_principle.md`). e2e selector 표준 정착(id/role 우선), 라벨 중복(LoginPage 두 form) / outdated 텍스트(WCA/MPL/EAN/JCtrans) / ECONNREFUSED 선제 try-catch / i18n localStorage seed 적용. 4 spec files / ~28 insert / ~12 delete, 메인 SPA 0 변경. PR #8 머지(4d5f691). Positive delta 4건. 학습 4건 메모리 저장(failOnStatusCode 한계 / check→e2e 가림 패턴 / smartQuoteLanguage key / Header role=banner). |
| phase-1.5-seo-infra | 100% | 2026-05-05 | Plan, Report (Design/Analysis 미작성 — 사후 회복형 PDCA). PR #3(`feature/phase15-seo-infra`) 머지(b1bfd40). SEO 인프라(robots.txt + 동적 sitemap.xml + JSON-LD Organization/WebSite + Lighthouse SEO 게이트) + Deep-link prefill(`/quote?origin=...&dest=...&weight=...`, IATA→ISO2 매핑, mount 1회 적용, fail-safe). 본 세션 차단 9건 해소: workflow `permissions: pull-requests: write`, **Vercel Pro 업그레이드**(`jlinsights-projects` Hobby → `goodman-jways` Pro) + Authentication OFF, lighthouserc url SPA 단축, `lighthouse:recommended` preset 제거, libxml2-utils 설치, preview 전용 `lighthouserc.preview.json` 분리(staticDistDir 충돌), preview SEO `error→warn`(Vercel 자동 noindex로 0.90 미달성), main 머지 충돌 2회. CI 11/11 PASS. tsx 런너 `scripts/render-sitemap.mts` 패턴 신규. 트레이드오프: production SEO schedule 워크플로 + color-contrast a11y 별도 사이클 필요. |
| production-seo-schedule | 100% | 2026-05-05 | Plan, Design, Analysis, Report (4문서 완비). PR #10(`feature/production-seo-monitor`) 머지(96c0069). phase-1.5 의 preview SEO warn 트레이드오프 해결 — bridgelogis.com 대상 Lighthouse 모니터(`.github/workflows/seo-production-monitor.yml` + `lighthouserc.production.json`). Trigger: push to main + 매주 일요일 17:00 UTC + workflow_dispatch. 회귀 시 GitHub Issue 자동 생성(sticky title 일일 idempotent). Wait step 90s sleep + curl 200 폴링 5분(push only). 첫 자동 실행(run 25359274803) PASS — Open Q 3건 모두 positive 해소(CSP 미차단 / 90s 충분 / **production accessibility 0.90 통과** → color-contrast 사이클 우선순위 재평가 자료). 단일 일자 사이클(plan→report 2026-05-05). Match Rate 100% (initial=final, 0 iterations). |
| smart-quote-history-table-refactor | 95% | 2026-05-20 | Plan, Design, Analysis, Report (4문서 완비). PR #18(`refactor/history-table-split`) squash 머지(e781483). Parent: `code-analysis 2026-05-15` Health 7.8/10 **P1 #3** close. `QuoteHistoryTable.tsx` 258 라인 단일 컴포넌트 → router 36 + Parts 156(5 named export: StatusPill/SurchargeStaleBadge/ExpiryBadge variant/MarginText/RowActions) + MobileQuoteTable 126(Fragment) + DesktopQuoteTable 156(`<table>` 단독) + utils/expiry 25 + 단위 테스트 63(7 케이스 PASS) 분리. **DOM 등가성 hard 규칙**: 라우터 wrapper 2 div 보유, Mobile=Fragment, Desktop=`<table>` 단독 → 원본과 노드별 동일. **외부 인터페이스 보존 hard 게이트**: `__tests__/QuoteHistoryTable.test.tsx` 7/7 **무수정 PASS** (행동 기반 단정 — getByText/getAllByLabelText/.animate-pulse, DOM 구조 단정 0건). 검증 6/6 PASS: tsc clean, eslint clean, vitest 50 files 1437 tests PASS, build ✓ 6.23s. Soft 게이트 1건 미달: 라인 예산 net +270 → +304(+34, 113%), 사유 prettier 다중 라인 className + JSDoc 비-로직 — 게이트 영향 없음. Critical gap 0, iterations 0. CI 전부 PASS (check 58s / e2e 56s / lighthouse 1m26s / Vercel main+insights deploy ✓). 학습 4건 메모리화([[feedback-subagent-file-write-lie]] gap-detector 미생성 적발, [[feedback-refactor-line-budget-buffer]] prettier+JSDoc 비-로직 비용, advisor design-단계 가치, 행동 기반 테스트 강건성). 후속: P1 #4 quote-detail-modal-split. |
