BridgeLogis Smart Quote — Secure sign-in

Hi<%= " #{@user.name}" if @user.name.present? %>,

We received a request to sign in to BridgeLogis Smart Quote. For your security, this link expires in <%= @expires_in_minutes %> minutes and can only be used once.

Sign in securely:
<%= @magic_link_url %>

Security note:
If you did not request this email, you can safely ignore it. BridgeLogis will never ask you to forward this link or share verification details.

—
Goodman GLS · BridgeLogis Smart Quote
A secure quotation workspace for global logistics operations.
