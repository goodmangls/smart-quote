💱 기준환율 1350→1320 갱신 (2026-09-02 기준, 입력값 그대로 적용) — **같은 KRW 견적의 USD 표시가 +2.3% 오른다.** 파트너에게 나가는 USD 금액이 바로 움직이는 값이라 배포 전 인지가 필요하다. 📐 **정책이 바뀌었다(2026-09-02)**: 종전 `floor(송금환율 / 50) × 50` 50원 내림을 폐지하고 **입력값을 그대로 적용**한다. 구 정책은 내림으로 안전 마진을 자동 부여했지만(환율을 낮게 잡으면 USD 견적이 높아지고 실제 시장 환율로 환전 시 KRW 가 더 들어온다) **마진 크기가 그 주 시장이 50원 경계 어디에 떨어지느냐에 따라 1원~49원으로 들쭉날쭉했다**(1401→1400 이면 버퍼 1원). 이제 마진 판단은 숫자를 입력하는 사람에게 있다 — ⚠️**입력값이 곧 적용 환율이고 곧 마진이다.** 1320 이 송금환율 그 자체라면 버퍼는 0 이다. 🏷️ **출처 주석을 정정했다**: `// Manual: 하나은행 월요일 09시 송금환율 (날짜)` → `// 적용 기준환율 (2026-09-02 확인)`. 새 정책에서 이 숫자는 사용자가 준 값이지 **하나은행 월요일 09시 고시라는 근거가 없고**, 2026-09-02 는 수요일이라 "월요일" 이라는 표기 자체가 사실과 어긋났다. 확인할 수 없는 출처를 모든 견적이 의존하는 상수에 박아두는 것은 값이 틀린 것 다음으로 나쁘다. 🔧 스킬(`~/.claude/skills/fx-update/`)도 함께 개선했다(저장소 밖이라 이 커밋에는 안 들어간다): 위치 인자(`fx-apply.py 1320`) 지원, 50원 단위 제약 제거, **주석 전체를 재작성**(값·날짜만 갈아끼우던 구조라 라벨이 낡은 채 남는 게 위 문제의 원인이었다), 읽기 정규식을 "줄 전체 + 주석에서 날짜 추출" 로 완화해 문구가 바뀌어도 안 깨지게 했다. 구 정책은 `--market` 으로 남겨 명시할 때만 동작한다. 안전장치 유지·추가: **500~3000 밖은 거부**(이 숫자는 모든 USD 견적에 곱해지므로 오타가 조용히 지나가면 안 된다) · **5% 이상 변동은 경고 후 진행**(실제로 그만큼 움직이는 주도 있어 막지 않는다) · 적용 후 두 파일 재읽기 검증(앵커 불일치 시 exit 1). ⚠️**스킬 검증은 스크래치패드 픽스처로 했다** — 세션 초반에 `--market 1372` 를 라이브 저장소에 돌려 main 을 1350 으로 만들어 놓은 실수가 있었고, 세션이 거기서 끊겼다면 아무도 고르지 않은 환율이 남았을 것이다. 구 라벨 재작성·반복 적용 안정성(1320→1325 연속)·인접 상수(`DEFAULT_FSC_PERCENT`·`PACKING_LABOR_UNIT_COST`) 무손상을 픽스처에서 확인한 뒤에야 실제 저장소에 적용했다. ⚠️**환율은 FSC 와 달리 DB·Admin 위젯이 없어 상수뿐이다 — 배포해야 반영된다.** TS↔RB 를 교차 검증하는 장치도 없어 한쪽만 고치면 조용히 어긋나므로 `--check` 로 두 파일 일치를 확인했다. 검증: tsc 0, eslint 0, vitest **76파일 1770건**, rubocop 156파일 0 offense, rspec **589/0**. smart-quote-emax 도 **같은 값·같은 주석**으로 동시 처리했다.
