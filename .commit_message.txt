♻️ refactor(api): `:unprocessable_entity` → `:unprocessable_content` 전환 (72곳) — Rack 3.2.6 은 이미 `SYMBOL_TO_STATUS_CODE[:unprocessable_entity]` 를 **`nil` 로 비웠고** 호환 shim 으로만 422 를 돌려준다. Rack 이 shim 을 걷어내는 순간 422 를 쓰는 전 경로(`INVALID_INPUT`·`ZONE_NOT_FOUND`·파트너 API)가 깨진다. **버그 수정이 아니라 마감 전에 shim 의존을 끊는 작업**이고, wire 포맷은 그대로다. ⚠️**검증 설계가 핵심**: 컨트롤러와 스펙을 같은 커밋에서 바꾸면 "둘이 서로 합의했다"만 증명되고 wire 가 여전히 422 인지는 증명되지 않는다 → **컨트롤러 38곳만 먼저 바꾸고 스펙은 손대지 않은 채 rspec 588건 전부 통과**시켜 wire 동일성을 입증한 뒤 스펙 34곳을 정리했다. 발견 2건: ①**경고는 컨트롤러가 아니라 rspec matcher 가 내고 있었다** — 컨트롤러 변경 후에도 41건 그대로였고 스펙까지 바꾸자 0 이 됐다. 즉 컨트롤러 쪽은 조용히 shim 에 의존 중이었다(위험이 더 큰 쪽). ②**422 를 숫자로 고정한 스펙이 한 곳도 없었다** — 계약 전체가 심볼 이름에만 걸려 있어 다음 rename 때 조용히 움직인다. 저장소당 wire 가드 1건 추가(`response.status).to eq(422)`), 400 주입으로 양쪽 첫 RED 확인. reason phrase 는 심볼이 아니라 코드에서 나오므로 불변(`HTTP_STATUS_CODES[422]` 은 이미 `"Unprocessable Content"`). 범위: app 38 + spec 34 + 현행 설계문서 1. **`docs/archive/**`·플랜·분석 스냅샷 13곳은 그때 코드의 기록이라 보존**, `vendor/bundle` 젬 소스는 0건 변경(pathspec 으로 제외·검증). rspec 589/0, rubocop 156 files 0 offenses, deprecation 경고 41→0, 프론트 변경 0.
