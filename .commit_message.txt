📝 docs: CLAUDE.md·AGENTS.md 환율 정책 확장 + 하드코딩 수치 제거 — 두 문서를 같은 상태로 맞췄다. ①**Exchange rate policy** 가 "매주 월요일 수동 업데이트" 한 줄뿐이라 정책도 실행 주체도 없었다 → `floor(시장/50)×50` 공식과 내림인 이유(견적 USD=`KRW÷환율`), `/fx-update` 스킬이 main+emax 동시 처리, ⚠️FSC 와 달리 DB·위젯이 없어 **배포해야 반영**되고 TS↔RB 교차검증이 `--check` 뿐, ✅대시보드 이탈 경고(`evaluateFxDrift`)와 emax 5개월 방치 사고, 15원 임계값 근거(위젯=시장환율 vs 정책=송금환율), ⚠️위젯 테스트를 환율 상수 실값에 묶지 말 것으로 확장. ②**stale 수치를 전부 제거했다 — 같은 값이 두 군데 박혀 있었다**: `Market defaults` 줄과 구조 트리의 `rates.ts` 줄에 UPS 45.50·DHL 48.00·FedEx 39.75(넉 달 묵음, 실제는 47.00/42.50/46.50)가 중복돼 있었고, 한쪽만 고치면 나머지가 남는 구조였다. 테스트 개수도 `58 files, 1480 tests`(실제와 260여 건 차이)를 빼고 실행 명령(`test:coverage`·`test:e2e` 포함)으로 대체했다. **수치를 문서에 옮겨 적지 말라는 규칙 자체를 두 문서에 명시**했다 — emax CLAUDE.md 가 이미 같은 이유로 숫자를 빼두었던 처방을 main 에 적용. 문서만 변경, 코드·동작 변경 없음.
