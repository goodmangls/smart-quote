🔐 fix(auth): 매직링크 브루트포스 스로틀이 실사용 경로를 보호하지 않던 문제 — 라우트 정리하다 발견한 실제 보안 결함. `rack_attack.rb` 의 20회/분 규칙이 `req.get?` 로 걸려 있었는데 프론트는 2026-08-17 에 POST 로 전환됐다. 즉 **토큰 검증 경로가 일반 API 상한 300회/분에만 묶여 있었고**(15배 약한 경계), 매직링크 토큰은 제시하면 세션이 발급되는 자격증명 등가물이다. `req.post?` 로 교정. 배포 윈도우용으로 남겨뒀던 `GET /auth/magic_link/verify` 라우트도 제거 — 메일 링크는 `FRONTEND_URL/auth/verify?token=` 로 프론트를 가리키고 프론트가 주소창에서 토큰을 지운 뒤 POST 하므로(MagicLinkVerifyPage.tsx:26) 살아 있는 링크는 영향 없음(메일러 템플릿 확인함). ⚠️GET 스펙 6건에 POST 에 없는 커버리지 4가지(httpOnly refresh 쿠키·소비된 토큰·만료·빈 토큰)가 있어 **삭제가 아니라 POST 로 이관**했고, GET 이 404 이며 토큰이 소비되지 않음을 단언하는 스펙을 추가. 검증 함정 2개를 통과: ①테스트 환경은 rails_helper 에서 `Rack::Attack.enabled = false` 라 처음 쓴 스펙은 무효였다(POST 가 안 막힌 게 아니라 스로틀 자체가 관측 불가). `quote_api_rate_limit_spec` 의 기존 패턴(파일 단위 enable+reset+freeze_time)을 따라 재작성 ②**하네스 자기검증** — 기존 GET 규칙이 429 를 내는지 먼저 확인한 뒤에야 POST RED 를 신뢰. BE 588 examples green, rubocop 156파일 offense 0
