♿ fix(a11y): Rate Table Viewer 의 select 3개에 접근 가능한 이름 부여 — 캐리어·요율제품·표모드 선택기가 `<label>` 도 `aria-label` 도 없어 스크린리더에 이름 없는 콤보박스 3개로 읽혔다(WCAG 4.1.2). admin 위젯 중 유일한 사례(AuditLogViewer 1/3·CustomerManagement 1/2 는 aria-label 보유). ⚠️**CI 통과는 근거가 아니었다** — `e2e/accessibility.spec.ts` 는 `/` 와 `/login` 만 방문하고 `/admin` 은 아예 안 간다. 즉 검사에서 빠져 있던 것이지 통과한 게 아니다. e2e 로 admin 을 덮으려면 인증+백엔드가 필요한데 CI e2e 는 백엔드를 안 띄워 skip 될 뿐이라, 컴포넌트 레벨(RTL 이 규격대로 접근가능 이름을 계산)로 게이트를 세웠다. 테스트 4건: 세 선택기 개별 + **미명명 컨트롤 0 전수 단언**(후자가 핵심 — 앞으로 이름 없이 추가되는 컨트롤도 잡는다. 이번 건이 그렇게 들어왔다). 결함주입 불필요 — 수정 전 4건 전부 RED 였다. FE 1687 green, tsc·lint clean
