📝 CLAUDE.md↔AGENTS.md 드리프트 해소 + 계획·개수·커밋메시지 규약 신설 (parity fixture 23건 → 실제 24건) — 🔴**쌍둥이가 실제로 갈라져 있었다.** 두 파일은 1·3·5행만 달라야 하는데 **400행과 409~412행이 같은 사실을 각자 다른 문장으로** 쓰고 있었다. 내용 손실은 아니었지만 **한쪽에만 있는 근거가 생겼다** — 15원 임계값의 *이유*(위젯은 시장중간환율·정책은 송금환율이라 다른 숫자, TT 스프레드 ~1%≈14원, 송금환율이 더 높아 상승 이탈을 늦게 잡는 지연을 밴드가 메운다)는 **AGENTS.md 에만** 있었고, FSC 값이 넉 달 묵었다는 이력은 **CLAUDE.md 에만** 있었다. 좋은 쪽을 CLAUDE.md 로 흡수한 뒤 **AGENTS.md 를 손으로 고치지 않고 생성**했다 — 1·3·5행만 치환하는 스크립트라 드리프트가 원리적으로 불가능하다. 생성 직후 **6행 이후 완전 일치**를 프로그램으로 재확인했고(전체 차이 6줄 = 의도된 3쌍), 같은 검사법(`diff <(tail -n +6 CLAUDE.md) <(tail -n +6 AGENTS.md)`)을 문서에 박았다. 🔴**썩은 수치를 제거했다** — `parity fixture 23건` 이라 적혀 있는데 `shared/test-fixtures/calculation-parity.json` 의 실제 `fixtures` 는 **24건**이다. 24로 고치면 다음 fixture 추가에 또 썩으므로 **수치를 지우고 파일을 가리키게** 바꿨다. 이 문서는 이미 FSC 요율과 테스트 개수에 같은 방식을 쓰고 있었는데(FSC 는 UPS 45.50·DHL 48.00·FedEx 39.75 라는 넉 달 묵은 값이 실제로 남아 있었고, 테스트 수는 2026-08 기준 260여 건 어긋나 있었다) 원칙이 흩어져 있어 **「문서에 개수를 적지 않는다」 절로 통합**하고 무엇이 언제 썩었는지 이력 표를 남겼다. ✅**계획 문서 규약을 신설했다** — 오늘 정지 헤더를 단 두 파일(`2026-05-04-fsc-sync-tool` 은 `/fsc-update` 스킬로 **대체됨**, `2026-05-10-quote-history-fr04-fr07` 은 완료 보고서·`by_amount_range` scope·xlsx 경로로 **이미 구현 확인**)의 근거를 문서에 옮겨, 새 계획을 실행하기 전에 헤더부터 보게 했다. **계획을 `docs/archive/` 로 옮기지 말라**는 이유도 적었다 — 완료 보고서가 계획 경로를 직접 링크해 이동하면 깨진다. ✅**`.commit_message.txt` 충돌의 성질과 한계를 문서화했다** — 이 파일은 **최근 50 커밋 전부**에 등장하고(모든 커밋이 통째로 덮어쓴다) 오늘 하루에만 세 번 충돌했다. 중요한 건 `.gitattributes` 의 `merge=ours` 가 **부분 해결뿐**이라는 점이다: 커스텀 merge driver 는 로컬 `.git/config` 에 정의되고 `.gitattributes` 는 이름만 가리키므로 **GitHub 이 계산하는 PR 충돌 상태는 그대로**이고 CI 재실행 비용이 남는다. 진짜 해결인 추적 해제는 CLAUDE.md 가 규정한 규약 변경이라 하지 않았고, 대신 **한계를 명시하고 해소 절차**(`git merge origin/main` → `--ours`)를 적었다. ⚠️**이미 머지된 정지 헤더의 방향 문구도 고쳤다** — 두 계획 파일에 `위 "For agentic workers" 지시는 무시하세요` 라고 썼는데 헤더가 맨 위라 지시문은 **아래**에 있다. 에이전트 혼동을 막으려는 파일에서 방향이 틀리면 안 된다. 변경은 `CLAUDE.md`·`AGENTS.md`·계획 2개, **전부 마크다운이고 코드 변경 0건**임을 `git diff --name-only` 로 확인했다.
