📝 Render 자동배포 상태를 실측값으로 교정 (미관측 → 발화 확인) — 어제 이 문단에 **"push 트리거가 실제로 발화하는지는 아직 관측되지 않았다"** 고 적었는데, 오늘 FSC 커밋(`rates.rb` 변경)이 처음으로 트리거 조건을 만족해 **관측됐다: 머지 5초 뒤 배포 시작, 1분 36초 뒤 success.** 2026-09-06 에 goodmangls org 에 Render GitHub App 을 설치한 것이 실제로 문제를 해소했다(원인은 배포 로그의 `we don't have access to your repo`). **"Manual Deploy 상시 필요" 지침을 폐기**했다 — 남겨두면 다음 사람이 매번 불필요한 수동 배포를 한다. 함께 적은 것: **발화 확인법**(Render GitHub App 이 GitHub Deployment 를 만든다 — `gh api repos/.../deployments?sha=$(git rev-parse HEAD)` 에서 `environment` 가 `main - smart-quote-api` 이고 status 의 `target_url` 이 `dashboard.render.com` 이면 확정)과, ⚠️**`smart-quote-api/` 아래가 바뀌어야 트리거된다**는 점(`render.yaml` 의 `rootDir`). 후자를 적어두는 이유는 **어제 문서 커밋으로는 이 검증을 할 수 없었기 때문**이다 — 프론트만 바뀐 머지가 Render 를 안 건드리는 건 정상이지 고장이 아닌데, 그걸 모르면 "또 안 뜬다" 로 오독한다. ⚠️**emax 는 다르다** — Render 가 GitHub 에 배포 기록을 남기지 않아 같은 방법으로 관측되지 않는다(이번에도 Vercel 배포만 보였다). 확인되지 않은 것을 확인된 것처럼 쓰지 않으려고 "대시보드에서 확인할 것"으로 남겼다. 개인 스킬 두 개(`~/.claude/skills/fsc-update/SKILL.md` · `fx-update/SKILL.md`)의 같은 문장도 함께 고쳤다 — 저장소 문서만 고치면 주간 작업 때 스킬 쪽 옛 지침을 먼저 읽게 된다. 코드 변경 없음.
