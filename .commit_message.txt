♿ fix(a11y): Surcharge 관리 폼 11개 필드에 접근 가능한 이름 부여 — 각 필드 위에 시각적 라벨은 있었지만 `htmlFor` 없는 **형제 `<label>`** 이라 스크린리더에는 이름 없는 입력 11개로 읽혔다(WCAG 4.1.2). `placeholder` 는 대체재가 아니다(입력하면 사라지고 지원도 일관되지 않음). ⚠️12개 중 "Active" 체크박스 1개만 `<label>` 이 input 을 감싸 정상이었다 — 코드를 읽고 "12개 전부"라고 단정했다가 테스트로 11개임을 확정했다. **aria-label 이 아니라 `useId` + `htmlFor`/`id` 로 기존 시각 라벨을 연결** — 시각적 이름과 접근가능 이름이 일치하고(WCAG 2.5.3) 라벨 클릭 시 필드가 포커스된다(클릭 타깃도 커짐). `useId` 를 쓴 이유는 폼이 한 화면에 두 번 렌더될 때 id 충돌을 막기 위함이고, 고정 문자열로 바꾸는 결함주입으로 해당 테스트가 실제 판별함을 증명했다. 테스트 15건 신규(테스트가 아예 없던 파일): 필드 12개 개별 + **미명명 컨트롤 0 전수 단언** + id 유일성 + 2회 렌더 id 충돌. 전수 단언이 핵심 — 앞으로 이름 없이 추가되는 필드도 잡는다(11개가 그렇게 쌓였다). 수정 전 13건 RED. FE 1712 green(1697→+15), tsc·lint clean
