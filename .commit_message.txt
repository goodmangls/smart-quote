🛡️ chore(deps): Rails 8.1.3.1 로 이관 + 워크트리 gitlink 제거 + 2026-08-21 분석 종결 — **인용돼 온 "Rails EOL 2026-10-07" 은 틀렸다.** `rubyonrails.org/maintenance` 1차 확인 결과 ①8.0.x **버그픽스 지원은 2026-05-07 에 이미 끝나 있었다**(08-21 분석 시점에 3.5개월 경과, 아무도 안 보고 있었다) ②보안 종료는 **2026-11-07** 로 인용값의 월·일이 뒤바뀐 것으로 보인다. 두 분석이 연달아 "1차 출처 미확인이니 사실로 인용하지 말 것"이라고 정확히 판단해 놓고 **확인은 하지 않은 채** 두 번 넘겼고, 그 사이 지원 종료 버전으로 굴러갔다 — 웹 조회 한 번이면 되는 일이었다. 조치: `~> 8.0.4` → `~> 8.1.3`, 락 8.0.5.1 → **8.1.3.1**(보안 2027-10-10 까지). ⚠️**`config.load_defaults 8.0` 은 일부러 유지한다** — 젬만 올리고 프레임워크 동작은 고정해 회귀면을 좁혔다. `--conservative` 라 **rails 계열 13개 젬만 이동하고 나머지는 한 줄도 안 움직였다**(puma `~> 7.2.1` 핀 무손상 — 풀면 운영 웹서버가 메이저 점프한다). 검증: rspec **589/0**, rubocop 156파일 0 offense, 커버리지가 업그레이드 전후 **완전히 동일**(1618/1939 · 456/698), **신규 Rails deprecation 0건**(남은 경고는 SimpleCov `add_group`/`add_filter` 로 Rails 무관·기존건). ⚠️8.1.x 의 버그픽스 지원은 2026-10-10 까지다 — 다음 시리즈가 나오면 또 올려야 한다. 🧹 **워크트리 2개가 gitlink(mode 160000)로 커밋돼 있었다** — `.claude/worktrees/{affectionate-gates-4ef910,gallant-fermi-c9cae1}`. `git add .claude` 가 각자 git 저장소인 워크트리를 서브모듈처럼 기록한 것이고, 브랜치를 지우는 순간 **도달 불가 SHA 를 가리키는 깨진 항목**이 되어 클론하는 사람마다 `git submodule` 로도 해소 안 되는 참조를 받는다. 인덱스에서 제거 + `.gitignore` 에 `.claude/worktrees/` 추가로 재발 차단(emax 는 같은 문제 없음 — 확인함). 삭제 전 **미커밋 변경까지 확인**했다(`cherry`·`diff` 는 커밋된 트리만 본다): 폐기분은 bkit 세션 카운터 2줄·**구버전 AGENTS.md 19.6KB**(main 31.3KB 의 옛 사본)·kamal vendor 젬 삭제 잔재뿐. 살릴 값어치가 있던 `docs/04-report/features/margin-formula-clarification.report.md` 163줄만 main 으로 옮겼다(이미 머지된 `df5b9c0` 의 PDCA 리포트, 참조하는 plan 문서는 main 에 이미 존재해 링크 유효). 📋 **2026-08-21 분석 미해결 4건은 전부 이미 닫혀 있었고 문서만 낡아 있었다** — 코드에서 직접 확인해 근거와 함께 표기: MEDIUM-2(락 `mail 2.9.1`, bundler-audit 0건) · MEDIUM-3(`NUMERIC_INPUT_BOUNDS` 로 FE Zod 미러, `fscPercent:100000`→840,196,400원 경로가 422 로 막힘) · LOW-4(`GET magic_link/verify` 제거, POST + 테스트가드 peek 만 잔존) · LOW-5(드롭다운 제거가 아니라 **존 맵 파생** `UNSERVICEABLE_COUNTRY_CODES` — `CW` 처럼 한 캐리어만 되는 곳은 제외해야 "캐리어를 바꿔 보라"가 유효한 조언으로 남는다). "검토 안 함"으로 남겼던 `MobileLayout`·`RateTableViewer` 도 테스트가 생겼다. ⚠️**남은 2건을 문서 상단에 명시**: ①🔴 `SELECT count(*) FROM quotes WHERE fsc_percent = 0` — **Render DB 접근이 없어 여전히 미확인**이고 이 문서에서 유일하게 고객 금액이 걸린 항목이다(해당 견적은 화면에 약 24.7% 높게 표시되고 저장은 낮게 됐다) ②`bundler-audit` CI 게이트 미도입 — emax 가 `mail` CVE 를 7일 방치한 게 이 게이트가 막았을 사례다.
