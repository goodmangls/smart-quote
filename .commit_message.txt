💰 fix(quote): 입력값 0 의 의미를 FE·BE 일치 — 화면 견적과 저장 견적이 24.7% 어긋나던 P1 해소. ①fscPercent: FE quotePricing 이 `||` 를 써서 명시적 0 을 캐리어 기본값(UPS 44.25%)으로 바꿔치기 → `??` 로 교정(같은 함수의 marginPercent 는 이미 `??`). 같은 입력이 화면 1,355,800 / 저장 1,020,600 으로 갈리던 것을 양쪽 1,020,600 으로 일치. FSC 입력칸은 빈칸과 0 을 구분하도록 표시용 draft 상태 도입 — 빈칸은 캐리어 기본값, 타이핑한 0 은 0% 라 실수로 칸을 비워도 할증료가 통째로 빠지지 않음 ②exchangeRate: Ruby `||` 는 0 이 truthy 라 BE 만 0 을 그대로 써서 Infinity→ActiveSupport 가 조용히 null 로 직렬화(파트너 API 응답 totalQuoteAmountUSD 누락). validate_quote_input! 에서 422 INVALID_INPUT 으로 거절(calculate·create·파트너 3경로 공통). 가드는 `present?` 가 아니라 `!nil?` — `"".present?` 는 false 인데 `"" || DEFAULT` 는 ""를 유지(Ruby 에서 "" 도 truthy)하고 `"".to_f`=0.0 이라 같은 구멍이 남았다. ""·"abc" 도 거절, 생략·null 은 기본값. 파트너 경로는 매퍼가 `.presence` 로 ""를 미지정으로 정규화하므로 201 — 직접 호출과 다른 이 차이도 스펙으로 고정 ③게이트: 공유 파리티 픽스처 ups_us_fsc_zero_explicit 신설(양쪽 원 단위 expected 단언) — FE 수정 되돌리기 결함주입으로 1355800≠1020600 RED, expected +100 주입으로 BE RED, BE `.nil?`→`blank?||zero?` 주입으로 3중 가드 RED 확인. 스냅샷 diff 48줄 추가·0줄 삭제로 기존 견적 금액 무변경 입증 ④defaultFscFor 를 config/rates.ts 로 단일화(quotePricing·CarrierComparisonCard·QuoteCalculator 3중복 제거). FE 1673·BE 573 green, 커버리지 FE 54.91%·BE 83.33%. CLAUDE.md/AGENTS.md 에 "입력값 0 의 의미" 규정 신설
