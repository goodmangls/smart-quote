🎯 feat(quote): Admin 기본 마진 0% 통일 + 마진↔총 견적 라이브 연동 + 저장 견적의 저마진 표시 — **관리자가 필드를 건드리지 않으면 아무도 고르지 않은 24% 가 실린 견적이 나가고 있었다.** 마진은 관리자의 판단이지 화면이 대신 정할 값이 아니므로 시작점을 0 으로 바꾸고(`ADMIN_DEFAULT_MARGIN_PERCENT`), 그 대신 **지금 이 % 가 만드는 금액과 고객에게 나갈 총 견적을 필드 바로 아래에 실시간으로** 띄운다. 계산이 `useMemo` 라 디바운스 없이 갱신되므로 **숫자가 타이핑에 따라 움직이는 것 자체가 설명**이 된다. 0% 일 때는 문구가 "원가 수준" 으로 바뀌어 값을 넣으라는 신호가 된다(⚠️매출은 100원 단위 올림이라 "총 견적 = 원가" 라고 단정하지는 않는다). ✅**0 왕복은 이미 양쪽에 고정돼 있어 안전하다** — FE `quotePricing.test.ts`(0 → profitMargin·profitAmount 0, `undefined` → 15), BE `quote_input_defaults_spec.rb` "keeps a margin of 0 instead of falling back to 15". Ruby 는 0 이 truthy 라 `|| 15` 가 0 을 보존하고(JS `||` 와 반대), `MarginRuleResolver` 는 파트너 API 경로에만 개입한다. 이 확인 없이 기본값만 바꿨다면 **화면은 원가, 저장은 15% 마진**으로 갈렸을 것이다(`fscPercent: 0` 이 화면 1,355,800 / 저장 1,020,600 으로 갈렸던 사고와 동형). 🔁 **초기화 버튼이 "무조건 0" 을 깨뜨리고 있었다(리뷰 발견).** 리셋은 `setInput(INITIAL_INPUT)` 인데 그건 회원용 15% 이고, admin 기본값은 `[isAdmin]` 의존 effect 라 **역할이 안 바뀐 리셋에서는 재실행되지 않는다** — 리셋 한 번이면 관리자가 15% 로 돌아갔다. `initialInputFor(isAdmin)` 로 해소. 곁가지로 **`hasManuallyChangedMargin.current` 가 리셋 시 초기화되지 않아 회원도 리셋 후 룰 자동해석이 계속 막혀 있었다** — 리셋이 리셋이 아니었다. 🚨 **저장된 견적의 저마진을 눈에 보이게 했다(사용자 요청).** 기본값이 0 이 되면서 "마진 입력을 잊은 견적" 이 정상 경로가 됐으므로, 승인 임계값(10%) 미만을 이력에서 식별할 수 있어야 한다. 기존에는 목록의 `MarginText` 가 **색상만** 바꿨고(dense table 에서 놓치기 쉽고 두 색을 구분 못 하는 사람에겐 아무 정보가 없다) **상세 모달에는 신호가 아예 없어** 저마진 견적이 정상 견적과 똑같이 읽혔다. `LowMarginBadge`(파일에 이미 있던 `SurchargeStaleBadge` 와 같은 amber 배지 어휘)를 목록·상세 양쪽에 붙이고, 배지 `title` 에 임계값과 "approval required" 를 적어 왜 떴는지 알 수 있게 했다. 📏 **임계값을 공유 상수로 뽑았다** — `LOW_MARGIN_THRESHOLD_PERCENT`·`isLowMargin`(`business-rules.ts`). 10 이 계산기 경고와 `MarginText` 에 각각 하드코딩돼 있었고 상세 모달엔 없었다. `quote_calculator.rb` 와 반드시 같은 값이어야 하며, 어긋나면 같은 견적이 한쪽에선 정상 다른 쪽에선 플래그로 읽힌다. `MetricCard.value` 는 배지를 넣기 위해 `string` → `ReactNode` 로 넓혔다. ⚠️**`resolvedMargin` 힌트를 "적용 룰" → "참고 룰" 로 바꿨다** — 관리자 경로는 자동 룰 해석을 건너뛰는데 힌트만 `적용 룰: X → 19%` 라고 떠서 0 이 든 입력칸과 정면으로 모순됐다. ⚠️**렌더 경로가 둘이다** — `QuoteCalculator`(데스크톱)·`MobileLayout`. 둘 다 금액을 내려준다. 한쪽만 했으면 모바일에서 힌트가 조용히 사라진다. `effectiveMarginPercent` 는 `FinancialSection` 까지 전달되면서 구조분해조차 안 된 죽은 prop 이었고, 필요한 건 %가 아니라 금액이라 `marginAmount`·`totalQuoteAmount` 를 새로 넘긴다. 📦 상수·헬퍼를 `src/pages/quoteDefaults.ts` 로 분리 — 컴포넌트 파일에서 export 하면 `react-refresh/only-export-components` 로 lint 가 막는다. 🌐 i18n **4개 언어 전부**(ko·en·cn·ja) 키 3개 추가. 인접 코드의 `isKo ? … : …` 2언어 축약은 따라가지 않았다(cn·ja 에 한국어가 보인다). 신규 테스트 19건(마진 UI 11 + 저마진 신호 8). **결함주입 6종으로 판별력 실증**: ①기본값 24 복원 → 1 RED ②라이브 금액 블록 제거 → 1 RED ③"참고 룰"→"적용 룰" → 1 RED ④`initialInputFor` 가 역할 무시(원래 리셋 버그 재현) → 2 RED ⑤저마진 배지 제거(색상만 = 원래 상태) → 2 RED ⑥임계값 경계 `<` → `<=` → 2 RED, 복원 후 전건 GREEN. ⚠️금액 픽스처는 `rates.ts` 어디에도 없고 15·24 와도 겹치지 않는 값(313131/929292)을 **포맷된 문자열로** 단언해 하드코딩·미포맷 회귀까지 잡는다. ⚠️기존 `FinancialSection.test.tsx` 는 `t` 를 항등함수로 mock 해 `{amount}` 치환이 사라지므로, 새 스위트는 실제 템플릿 mock 을 따로 둬 **보간 자체를 검사**한다. ⚠️**이력의 마진 열에는 역할 게이팅이 없어 회원도 마진율을 본다**(기존 동작). 배지는 이미 보이던 숫자를 말로 옮긴 것이라 새로 새는 정보는 없지만, 이력 마진을 admin 전용으로 할지는 별건 판단이 필요하다. 검증: tsc 0, eslint 0, vitest **78파일 1789건**(76/1770 → +2파일 +19건, 새 `.tsx` 수집 여부를 개수로 확인), rubocop 156파일 0 offense, rspec **589/0**(백엔드 무변경).
