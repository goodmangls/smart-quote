⛽ UPS·DHL·FedEx FSC 2026-09-14 주간 요율 반영 (UPS 49.75% / DHL 43.75% / FedEx 49.0%) — 46.75/43.5/46.0 → 49.75/43.75/49.0. UPS·FedEx 가 각각 **+3.00** 로 크게 올랐다. **이 커밋은 견적 금액을 바꾸지 않는다.** 적용 요율의 출처는 DB(`fsc_rates`)=Admin 위젯이고, 여기서 고치는 파일들은 **DB 조회 실패·요청 대기 중 폴백**이자 이력 차트 시드다. 프로덕션 DB 는 프로브 시점에 아직 46.75/43.50/46.00 이었다 — **위젯 입력이 선행돼야 견적이 움직인다.** ✅**DHL 43.75% 는 공식 페이지 파싱과 교차 확인했다** — `9월 14-20, 2026 / 43.75%` 밴드로, 사용자 제공값과 정확히 일치한다. 같은 페이지에 다음 주(9월 21-27) **45.00%** 가 이미 게시돼 있다. UPS·FedEx 는 봇 차단이라 사용자 제공값이다. ✅**DB 현재값은 추측하지 않고 프로덕션 응답으로 역산했다** — 계산 API 를 `marginPercent:0`·`fscPercent` 생략으로 호출해 `intlFsc/intlBase` 를 계산했고(마진 0 이어야 `base_with_margin==intl_base` 라 비율이 곧 요율), ⚠️**대조군으로 명시 `fscPercent:50` 을 같이 쏴 정확히 50.00% 가 나오는 것까지 확인**했다 — 이게 없으면 프로브가 요율이 아닌 걸 재고 있어도 모른다. 🔒**어제 만든 rates 드리프트 게이트(#115)가 오늘 첫 실전에서 작동했다** — `DEFAULT_FSC_PERCENT*` 를 스냅샷에 넣어둔 덕에, `rates.rb` 만 고치고 스냅샷을 재생성하기 **전에 BE·FE 양쪽이 RED** 였다(`1 example, 1 failure` / `1 failed | 4 passed`). 게이트가 없었다면 조용히 통과했을 상태다. 문서화된 절차(`.rb` 수정 → `bin/rails tariff:snapshot` → `.ts` 미러링)를 그대로 따랐고, 스냅샷 diff 가 **정확히 FSC 3값**으로만 한정됨을 트리 비교로 확인했다(다른 요율표 무변경). 검증: tsc 0, eslint 0, vitest **1803/1803**, rubocop 159파일 0 offense, rspec **609/0**. ⚠️1802 → 1803 은 `fsc-history.test.ts` 가 주차 체크포인트를 하나 추가했기 때문이다 — 이 테스트는 **의도적인 사람-확인 장치**라 갱신하지 않으면 RED 가 된다. ⚠️두 저장소의 vitest 를 **동시에 돌리지 않고 순차로** 실행했다. smart-quote-emax 도 **같은 값·같은 날짜**로 동시 처리했다 — 두 저장소는 같은 캐리어 공시값을 쓰므로 `fsc-apply.py` 기본값이 `--repo both` 다. ⚠️**emax 에는 이 스냅샷 게이트가 없다**(`shared/tariff-snapshots/` 도 `tariff:snapshot` rake 도 부재). 이번엔 스크립트가 TS·RB 를 함께 써서 어긋날 일이 없지만, 손으로 고칠 때는 잡아줄 장치가 없다.
