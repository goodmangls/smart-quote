🛡️ fix(api): 백엔드 숫자 입력 범위 검증 — 프론트 Zod 를 우회하는 호출이 금액 계산에 무검증으로 도달하던 구멍 차단. 실측(292,000원 기준 견적 대비): `fscPercent: "abc"`→232,100(`"abc".to_f`=0.0 이라 연료할증료가 통째로 0), `fscPercent: 100000`→200,105,400, `dutyTaxEstimate: -500,000`→**-208,000(음수 총액)**, `manualSurgeCost: -300,000`→**-8,000**, `pickupInSeoulCost: -200,000`→92,000. 계산·저장의 권위자는 백엔드인데 범위 검증의 실질적 주체는 FE 하나뿐이었다. QuotesController::NUMERIC_INPUT_BOUNDS 단일 테이블로 9개 필드를 calculate·create·파트너 3경로에 동시 적용하고, 기존 exchangeRate 전용 가드도 여기로 흡수. ⚠️핵심: **범위 검사만으로는 부족하다** — `"abc".to_f`·`"".to_f` 가 모두 0.0 이라 문자열이 "유효한 0" 으로 통과하므로 NUMERIC_INPUT_PATTERN 으로 숫자 여부부터 거른다(exchangeRate `""` 구멍과 같은 부류). 의도적 제외 2건: marginPercent 는 계산기가 이미 0..MAX_MARGIN_PERCENT 로 clamp 해 금액이 묶여 있고, 품목 수는 FE 가 폼이라 100 으로 막지만 파트너 화물은 150개도 정상이라 상한을 두지 않았다(파트너 API 는 키별 레이트리밋 존재). manualPackingCost 는 계산기가 음수를 조용히 무시하던 것을 422 로 전환 — 무음 무시보다 거절이 낫다. 결함주입 2종으로 게이트 증명: 비숫자 검사 제거 시 "abc" 케이스만 RED(범위 검사가 못 잡음을 입증), 하한 검사 무력화 시 7건 RED(파트너 경로 2건 포함 — 매퍼를 읽는 대신 스펙으로 확인). BE 587 examples green(573→+14), 커버리지 83.44%, rubocop 155파일 offense 0. CLAUDE.md/AGENTS.md 에 단일 출처·문자열 함정 명시
