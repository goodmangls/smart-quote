📝 요율 갱신 절차와 테스트 함정을 CLAUDE.md·AGENTS.md 에 명문화 — 오늘 UPS Document Tariff 교체(#113)와 RouteSection 결함 수정(#112)에서 **문서에 없어서 매번 코드를 뒤져야 했던 것들**을 적었다. 코드 변경 없음. **① 요율 갱신 절차** — 기존 문장은 `Tariff sync: FE 파일은 BE 와 동기화되어야 함` 한 줄뿐이라 **실제 절차가 어디에도 없었다.** `shared/tariff-snapshots/tariffs.json` 게이트의 존재와 **`.rb` 수정 → `bin/rails tariff:snapshot` → `.ts` 미러링** 순서를 적었다(스냅샷을 건너뛰면 게이트가 옛 값을 들고 있어 양쪽 다 빨개진다). 이 절차는 `tariffParity.test.ts` **주석에만** 있었고 CLAUDE.md 만 읽으면 알 수 없었다. ⚠️**100셀짜리 표를 손으로 옮기지 말 것**(PDF 파싱해 `.rb`·`.ts` 를 같은 결과로 생성, 구조 불일치 시 중단)도 함께 적었다 — 타이핑은 고객에게 틀린 금액이 나가는 경로를 셀 수만큼 만든다. 🔴⚠️**요율 테스트의 기대값 리터럴을 상수 참조로 "정리"하지 말 것** — 리터럴이라야 요율 변경 시 RED 가 되어 사람이 캐리어 시트와 재확인하고, 실제로 오늘 UPS Document 교체를 이 리터럴이 잡았다. 같은 파일의 DHL 단언이 `DHL_DOC_EXACT_RATES.Z1[1]` 을 참조해 표가 틀리게 바뀌어도 통과하는 **대비 사례**라 함께 적었다. **② 테스트 함정 2건** — ⚠️**큰 목록에 `getAllByRole` 금지**: 206개 `<option>` 기준 400ms~2.6초로 6배 요동(같은 결과인 `querySelectorAll` 은 0.5ms)해 5초 타임아웃을 산발로 넘겼고, **단독 실행과 CI 에서는 재현되지 않아** 오래 방치됐다. ⚠️**비결정 실패는 한 번의 green/red 로 판정하지 말 것**: 같은 트리에서 실패→실패→통과가 나오므로 원인 귀속은 **HEAD 반복 실행**으로 가른다(#112 에서 이 방법으로 "내 변경 탓"이라는 오판을 정정했다). **③ emax 와의 분기** — UPS Document 요율이 smart-quote-emax 와 다른 것이 정상임을 적었다(2026-09-08 확인). 근거 없이 두면 다음 사람이 "emax 가 뒤처졌다"고 보고 맞추려 든다. ⚠️**twins 동기화를 실측 확인했다** — 두 파일에 같은 내용을 넣고 diff 줄 수가 16(기존 드리프트)에서 늘지 않았음을 확인했다. 중간에 CLAUDE.md 에만 링크 문구를 넣어 18줄로 벌어졌던 것을 되돌렸다. 문서에 적은 주장은 파일 존재로 검증했다(`tariff_snapshot.rake` · `tariffs.json` · `tariff_snapshot_parity_spec.rb` 존재). smart-quote-emax 의 CLAUDE.md·AGENTS.md 에도 대응 내용을 넣었다 — 그쪽엔 **스냅샷 드리프트 게이트가 아예 없다**는 점(TS↔RB 불일치를 잡는 장치 부재)도 함께 적었다.
