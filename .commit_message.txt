🚨 fix(fsc): Admin 위젯 저장 실패가 조용히 묻히던 결함 정정 — **일부 캐리어만 반영된 채 관리자가 성공으로 오인할 수 있었다.** emax 에서 발견해 고친 결함군이 main 에도 그대로 있었다(emax 위젯이 main 을 이식한 것이라 뿌리가 같다). `handleSave` 의 `catch {}` 에 `// error surfaced via useFscRates error state` 라 적혀 있었는데 **그 주석이 거짓이다** — `useFscRates.error` 는 `GET /api/v1/fsc/rates` 실패만 담고 `POST /api/v1/fsc/update` 실패는 어디에도 반영되지 않는다. 캐리어당 별도 POST 라서 2번째(DHL)에서 터지면 UPS 는 **이미 DB 에 기록되고** DHL·FEDEX 는 옛 값으로 남는데, 화면에 아무 메시지도 없고 `setIsEditing(false)` 도 `fetchRates()` 도 실행되지 않아 편집폼만 그대로 떠 있다. 관리자는 저장된 줄 알고 떠나고 **그 순간부터 캐리어마다 다른 주차의 요율로 견적이 나간다** — FSC 는 총액에 직접 곱해지므로 조용한 금액 오차가 된다. 정정 4가지: ①`saveError` 상태 + `role='alert'` 배너로 실패를 화면에 노출하고 `Sentry.captureException` 으로 원격 보고 ②**캐리어를 세 갈래로 구분해 알린다** — 실패 이전은 성공 응답을 받았으므로 `저장 확정`, **실패한 캐리어는 `반영 불확실`**, 그 뒤는 요청 자체를 안 보냈으므로 `이전 요율 확정`. ⚠️요청이 throw 했다는 건 **클라이언트가 응답을 못 받았다는 뜻일 뿐**이라 타임아웃·소켓 끊김·502 는 서버가 커밋한 뒤에도 발생한다 — "실패=미반영" 으로 단정하면 관리자에게 알 수 없는 것을 안다고 말하는 셈이다. `inFlight` 를 비관적으로 들고 있다가 throw 시 어느 캐리어가 미확인인지 특정한다 ③**실패해도 `fetchRates()` 를 다시 돌고** 각 입력칸 아래에 실제 DB 값(`현재 DB: 22.22%`)을 노출한다 — 실패 시 편집 모드를 유지하므로 셀은 관리자가 타이핑한 값만 그리고, 이게 없으면 "현재 DB 를 확인하라" 는 안내가 **화면에 없는 것을 가리킨다.** 편집 중에는 새로고침 컨트롤이 숨겨져 있어 **재조회 버튼**도 함께 넣었다(입력값 보존, 취소하지 않고 재확인 가능) ④**"현재 DB" 는 네 상태를 구분한다**: 확정(숫자) · `확인 중…`(loading) · `읽지 못했습니다`(read error) · `—`. ⚠️**이 저장소에서 특히 중요하다** — `useFscRates` 가 `data` 를 `DEFAULT_FSC_RATES`(= `rates.ts` 상수)로 **시드**하고 성공한 읽기에만 교체하므로, 조회 실패·진행 중에 값을 그대로 찍으면 **상수를 테이블 내용인 양** 보여주게 된다. `useFscRates.load()` 가 시작 시 `setError(null)` 을 하므로 재조회 버튼을 누른 직후가 정확히 그 창이다. ⚠️`catch` 안의 `fetchRates()` 는 try/catch 로 감싸지 않았다 — `retry` 는 자체 catch 로 `error` 상태에 흡수하고 **절대 reject 하지 않으므로** 감싸면 죽은 코드가 된다(emax 에서 실제로 죽은 코드였다). 접근성 겸 테스트용으로 편집/저장/취소/재조회 버튼과 요율 입력칸에 `aria-label` 을 붙였다. 신규 테스트 12건. **결함주입 3종으로 판별력 실증**: ①원래의 삼키는 `catch` 복원 → 8건 RED ②실패 캐리어를 "미시도" 로 뭉뚱그림 → 2건 RED ③loading/error 상태 무시 → 2건 RED, 복원 후 12건 GREEN. ⚠️요율 mock 은 `rates.ts` 에 없는 값(11.11/22.22/33.33)을 쓴다 — 실제 주간 요율이면 DB 를 읽든 시드를 읽든 통과해 검사가 무력해지고 `/fsc-update` 주마다 무관하게 깨진다. 검증: tsc 0, eslint 0, vitest **76파일 1770건**(75/1758 → +1파일 +12건, 새 `.tsx` 수집 여부를 개수로 확인), rubocop 156파일 0 offense(백엔드 무변경).
