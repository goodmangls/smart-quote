🔒 fix(share): 공개 공유 링크가 내부 원가·마진을 내보내던 누출 차단 — **견적을 받는 고객이 링크만 열면 우리 원가와 마진을 볼 수 있었다.** `GET /shared/:token` 은 `before_action :authenticate_user!, only: [ :create ]` 라 **조회에 인증이 없는데**, `QuoteSerializer.detail(quote)` 를 통째로 렌더해 `totalCostAmount`(내부 원가) · `profitAmount` · `profitMargin` · `marginPercent` 를 응답에 실어 보냈다. 공유 페이지가 그 값을 화면에 그리지 않아 **누출이 순전히 응답 본문에만 있었고**(DevTools·curl 로 읽힘) 눈으로는 드러나지 않았다. 조치: 고객 전용 `QuoteSerializer.shared` 를 **화이트리스트로** 신설했다. ⚠️`detail` 에서 `except:` 로 빼는 방식은 쓰지 않았다 — 그러면 나중에 `detail` 에 추가되는 필드가 **기본으로 누출**되고, 이번 사고와 똑같은 형태(존재하는데 아무도 안 보는 경로)가 반복된다. 고객이 볼 수 있는 것은 여기에 이름으로 적어야만 나간다. 프론트 계약 `SharedQuoteData`(`src/api/shareApi.ts`)와 1:1로 맞췄다. 🐛 **같은 원인으로 공유 페이지가 이미 깨져 있었다.** 계약이 요구하는 `overseasCarrier` · `transitTime` · `totalQuoteAmountUsd` 세 필드가 `detail` 에 **아예 없다**(detail 은 대문자 `totalQuoteAmountUSD` 를 쓴다). 즉 `SharedQuotePage` 의 `data.totalQuoteAmountUsd.toLocaleString(...)` 은 `undefined` 에서 터져 **USD 표시가 ErrorBoundary 로 떨어지고 있었다.** 새 serializer 가 계약대로 세 필드를 채우면서 함께 해소된다 — 누출을 막는 김에 고친 게 아니라, 둘 다 "계약을 안 지킨 응답" 이라는 한 원인의 두 증상이다. 신규 스펙 7건(`quote_shares_spec.rb` — 기존에 없었다). ⚠️**단언은 렌더 결과가 아니라 응답 본문에 한다** — 누출이 거기 있었기 때문이다. 원가·마진 4종 부재 · `breakdown` 부재(`intlBase` 는 마진 전 캐리어 요율이다) · 운영 필드 8종 부재에 더해, **키 집합을 정확히 일치시키는 단언**을 넣어 앞으로 필드가 늘면 반드시 의도적 결정을 거치게 했다. **결함주입으로 판별력 실증**: 컨트롤러를 `QuoteSerializer.detail` 로 되돌리자 원가·마진·breakdown 단언이 정확히 RED, 복원 후 7건 GREEN. 검증: rubocop 157파일 0 offense, rspec **596/0**(589 → +7), FE tsc 0 · eslint 0(프론트 무변경 — 계약은 이미 옳았고 백엔드만 그걸 안 지키고 있었다). 📌 이 커밋은 **보안 수정만** 담는다. 사용자가 요청한 "이력 마진을 admin 전용으로" 는 UI 게이팅이 필요해 별도 PR 로 분리했다 — 보안 수정이 UI 리뷰를 기다릴 이유가 없다.
