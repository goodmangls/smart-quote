⛽ UPS·DHL·FedEx FSC 2026-08-31 주간 요율 반영 (UPS 48.25% / DHL 43.25% / FedEx 47.75%) — 직전 주(2026-08-24) 대비 UPS +1.25pp · DHL +0.75pp · FedEx +1.25pp 로 세 캐리어 모두 인상. **DHL 은 공식 페이지에서 교차 검증했다** — `mydhl.express.dhl` 의 주간 밴드가 `8월 31-9월 6, 2026 → 43.25%` 로, 직전 밴드 42.50%·다음 밴드(9월 7-13) 43.50% 사이에 정확히 들어맞아 세 값이 같은 주차 세트임이 확인됐다. UPS·FedEx 는 봇 차단이라(2026-08-24 재확인, headed 로도 chrome-error) 페이지 확인 없이 사용자 제공값을 그대로 적용한다. ⚠️**이 커밋만으로는 견적 금액이 바뀌지 않는다** — 견적에 실제로 적용되는 요율의 단일 출처는 DB(`fsc_rates`) = Admin FSC 위젯이고(2026-08-24부터 FE `useCarrierFscDefault`·BE `default_fsc_for` 가 DB 우선), 여기서 고친 상수는 **DB 조회 실패·요청 대기 중 폴백**이자 이력 차트 시드다. 위젯 입력은 별도로 완료했다. 저장소당 4파일을 `fsc-apply.py` 로 한 번에 처리: `src/config/rates.ts`(TS `.5` 표기) · `smart-quote-api/lib/constants/rates.rb`(Ruby `.50` 표기) · `src/config/fsc-history.ts`(차트 시드 append) · `src/config/__tests__/fsc-history.test.ts`. **마지막 두 파일이 빠뜨리기 쉬워 스크립트로 묶여 있다** — `fsc-history.test.ts` 는 의도적인 사람-확인 체크포인트라 갱신하지 않으면 RED 가 되고, 시드↔상수 정합 테스트가 부분 갱신도 잡는다. 검증: tsc 0, eslint 0(`--max-warnings 0`), vitest **75파일 1758건 전건 통과**, rubocop 156파일 0 offense, rspec **589/0**. smart-quote-emax 도 **같은 값으로 동시 처리**했다(OCS 는 ad-hoc 주기라 25.0% @ 2026-07-20 유지, 손대지 않음).
