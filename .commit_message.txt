📌 낡은 계획 파일 2건에 "실행하지 말 것" 표시 (미완 체크박스 129개가 오독을 유발) — 오늘 `/subagent-driven-development` 를 부르려다 발견한 함정이다. `docs/superpowers/plans/` 의 계획 두 개가 **체크박스 129개 전부 미완**으로 보이는데 실제로는 둘 다 끝난 일이었다. 더 나쁜 건 두 파일 상단에 `**For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development ... to implement this plan task-by-task` 라는 **실행 지시**가 박혀 있다는 점이다 — 에이전트가 읽는 순간 이미 배포된 기능을 다시 만들기 시작한다. 🔴**`2026-05-04-fsc-sync-tool` (77개) — 대체됨.** 만들려던 CLI `tools/fsc-sync` 는 끝내 만들어지지 않았고 필요도 없어졌다. 같은 일을 개인 스킬 `/fsc-update`(`fsc-apply.py`)가 한다 — 두 저장소의 FSC 4파일씩을 한 번에 동기화하며, **오늘 2026-09-14 주간 갱신도 이걸로 처리했다.** 차이는 커밋·푸시를 스킬이 대신하지 않는다는 것뿐이다. 완료 보고서는 없다. 🔴**`2026-05-10-quote-history-fr04-fr07` (52개) — 이미 구현됨.** 체크박스가 미완인 게 아니라 **미갱신**이다. 근거 셋을 각각 확인했다: 완료 보고서 `docs/04-report/features/quote-history-fr04-fr07.report.md` 존재 · FR-04 의 `by_amount_range` scope 가 `quote.rb` 에 실재(오늘 코드 분석에서 직접 읽었다) · FR-07 xlsx 경로가 `src/features/history` 에 실재. ⚠️**옮기지 않고 제자리에 표시했다.** 처음엔 `docs/archive/2026-05/`(이미 존재)로 이동하려 했는데, **참조를 먼저 확인하니 `quote-history-fr04-fr07.report.md:307` 이 계획 파일 경로를 직접 가리키고 있었다** — 옮겼으면 그 링크가 깨진다. 이동 대신 상단에 정지 헤더를 넣어 링크를 보존했다. 각 헤더는 ①상태(대체/완료) ②체크박스가 무효·미갱신인 이유 ③무엇이 대신하는가 ④코드로 확인한 근거 ⑤`For agentic workers` 지시를 무시하라는 명시를 담는다. 검증: 새로 넣은 상대 링크 `../../04-report/...` 가 실제 파일로 해석되는지 `os.path.normpath` 로 확인했고, 기존 보고서→계획 참조도 파일을 안 옮겼으므로 그대로 유효함을 확인했다. 변경이 `docs/**.md` 2개로만 한정돼 코드 경로에 영향이 없음을 `git diff --name-only` 로 확인했다(그래서 전체 테스트 스위트는 돌리지 않았다). ⚠️**같은 함정이 남아 있을 수 있는 곳**: 이 저장소의 `docs/superpowers/plans/` 는 정리됐지만 smart-quote-emax 와 다른 프로젝트는 이번에 확인하지 않았다. ⚠️**별건으로 남긴 것** — `.commit_message.txt` 가 **최근 50 커밋 전부**에 등장한다(모든 커밋이 덮어씀). 브랜치가 둘 이상이면 충돌이 구조적으로 필연이고 오늘만 세 번 겪었다. `.gitattributes` 의 `merge=ours` 는 **부분 해결뿐**이다 — 커스텀 merge driver 는 로컬 `.git/config` 에 정의되고 `.gitattributes` 는 이름만 가리키므로 **GitHub 이 계산하는 PR 충돌 상태는 그대로**이고 CI 재실행 비용이 남는다. 진짜 해결은 추적 해제인데 그건 CLAUDE.md 가 규정한 규약 변경이라 임의로 하지 않았다.
