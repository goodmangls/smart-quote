🚦 chore(a11y): eslint-plugin-jsx-a11y 게이트 도입 — 접근가능 이름 누락이 반복해서 들어오던 **구조적 원인**(정적 검사 부재)을 막는다. 최근 2주에 같은 부류를 세 번 손으로 고쳤다(RateTableViewer #88·SurchargeForm #89, 그 전 MobileLayout). `e2e/accessibility.spec.ts` 는 `/`·`/login` 만 돌고 admin e2e 는 CI 에 백엔드가 없어 skip 되므로 게이트가 못 된다 — lint 가 맞는 층위다. **vitest.config.ts 의 커버리지 플로어와 같은 래칫 방식**: 기존 부채는 파일 단위로 명시 나열하고 목록은 줄어들기만 한다(빨간 빌드를 통과시키려고 파일을 추가하는 것 금지 — 새 파일이 목록을 필요로 한다는 건 그 변경이 라벨 수정을 같이 해야 한다는 뜻). 도입 시점 위반 28건/12파일을 셋으로 분류: ①`no-autofocus` 3건은 **부채가 아니라 의도** — 로그인 등 그 폼이 목적의 전부인 화면이라 전역 off ②모달 backdrop 6건(3파일)은 **규칙이 요구하는 수정이 오히려 나쁨** — 세 모달 모두 Escape 를 이미 처리하고, backdrop 에 키 핸들러를 달면 컨트롤 아닌 요소가 탭 순서에 들어간다. 해당 파일만 off 하고 다른 곳의 클릭 요소는 계속 검사 ③나머지 19건(7파일)이 진짜 부채 → 래칫 목록. **게이트 유효성 증명**: 목록에 없는 파일(RouteSection)에 미연결 label 주입 → `lint exit 1` + 정확한 규칙명. 설치 검증은 배포와 같은 경로(`npm ci`, 613패키지)로 했고 lock diff 는 **순수 추가 141줄·삭제 0**(다른 패키지 미이동). FE 1712 green, tsc·build clean
