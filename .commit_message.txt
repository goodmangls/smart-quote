📝 CLAUDE.md·AGENTS.md 를 이번 사이클 사실로 갱신 + 관리자 화면 목업 3종(`design/`) — 문서에서 **틀린 문장을 먼저 걷어냈다**. 가장 위험했던 건 환율 정책이다: 두 파일 모두 "하나은행 09시 송금환율을 50원 내림(`floor(시장/50)×50`)"이라고 적혀 있었는데 **2026-09-02 에 정책이 바뀌어 부르는 숫자를 그대로 적용**한다(`/fx-update 1300` → 1300). 구 규칙은 `--market` 플래그로만 남았다. 폐기 이유(버퍼가 시장 위치에 따라 1~49원으로 들쭉날쭉, 1401→1400 이면 1원)와 ⚠️**입력값이 곧 마진**이라는 점을 같이 적었다 — 스크립트가 아무 보정도 안 하므로 송금환율을 그대로 넣으면 버퍼가 0 이다. `DEFAULT_EXCHANGE_RATE` 옆 괄호도 `(하나은행 월요일 09시 송금환율)` → `(적용 기준환율)` 로 고쳤다. **smart-quote-emax 의 CLAUDE.md·AGENTS.md 도 같은 문장을 갖고 있어 4파일을 함께 고쳤다.** 새로 적은 것: **Admin 시작 마진 0%** 정책(`ADMIN_DEFAULT_MARGIN_PERCENT`)과 ⚠️Reset 이 이 규칙을 깨는 경로(`isAdmin` 키 effect 는 리셋 때 재발화하지 않아 `setInput(INITIAL_INPUT)` 이면 admin 이 15% 로 앉는다 — `initialInputFor(isAdmin)` + `hasManuallyChangedMargin` ref 비우기), 저마진 임계값 단일 출처(`LOW_MARGIN_THRESHOLD_PERCENT = 10`), **마진·원가의 deny-by-default 직렬화**(`include_margin:` 기본 `false`, 공유 링크는 화이트리스트 `QuoteSerializer.shared` — 미인증 경로라 담을 것을 고른다), FSC 위젯의 **부분 저장 실패 3분류**(저장됨 / 불확실 / 미시도)와 `현재 DB` 표기 규칙. Render 항목은 **관측한 것만** 적었다 — 원인은 확인했지만(`we don't have access to your repo`, goodmangls org 에 Render GitHub App 미설치 → 설치 완료) **push 트리거가 실제로 발화하는지는 아직 안 봤다**고 명시했다. 검증: 4파일에서 `50원 내림` 잔존 0건(구 정책 서술만 파일당 1건), 문서에 적은 심볼은 전부 코드에서 실재 확인(`hasManuallyChangedMargin` L134·`LowMarginBadge` L55·`QuoteSerializer.shared` 호출부·`EDITABLE_FSC_CARRIERS = ['UPS','DHL','FEDEX']`·`현재 DB: 확인 중…`). ⚠️쌍둥이 잔여 드리프트는 **표현 차이뿐이고 사실은 일치**한다(FX 4개 불릿의 문체). ⚠️`design/bridgelogis-admin-screens.html` 은 `seed-canvas.mjs` 생성물 2.4MB — 원본은 `.dc.html` 3개 + `canvas.json` 이고 커밋한다면 생성물은 제외할 것. ⚠️Geist 는 impeccable 훅이 `overused-font` 로 잡지만 **앱이 실제로 쓰는 폰트**라 바꾸지 않았다.
