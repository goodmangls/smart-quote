🎨 디자인 감사 Quick Wins 3건 — 랜딩 푸터 복구·모바일 터치 타깃 44px·로그인 h1 — `bridgelogis.com` 공개 3페이지(`/`·`/login`·`/guide`)를 실측 감사한 결과 중 **안전하고 검증 가능한 3건만** 적용했다. **① 랜딩에 푸터가 아예 없었다** — 페이지가 세 번째 기능 카드 설명("...Door-to-Door costs.")에서 그냥 끝나고 회사 정보·저작권·마무리 CTA 가 전무했다. 🔴**`Footer` 컴포넌트는 이미 있었고 i18n 키 이름도 문자 그대로 `landing.footer` 인데 랜딩에만 마운트되지 않았다**(`/guide` 에는 있었다). import 한 줄 + 마운트 한 줄로 해소. ✅**부수 효과가 더 크다** — 푸터 문구가 `© 2026 BridgeLogis...` 라서 **본문 내 "BridgeLogis" 등장이 0회 → 1회** 가 됐다. 그 전까지 `bridgelogis.com` 방문자는 "Smart Quote" 라는 다른 이름만 보고 회사명(KS Ways)은 어디에도 없었다. **② 모바일 터치 타깃** — 햄버거가 36×36 이었다(`p-2` + 20px 아이콘). 🔴**모바일에서 내비게이션에 닿는 유일한 입구다.** 테마 토글도 함께 올렸다. 패딩을 늘리는 대신 `min-h-[44px] min-w-[44px]` 를 명시해 아이콘 크기가 바뀌어도 44px 가 유지되게 했다. **③ `/login` 에 `h1` 이 없었다** — "Log in to your account" 가 시각적으로만 제목이고 태그는 `h2`, 위에 `h1` 이 없어 스크린리더에 페이지 제목이 없었다. 태그만 교체라 **시각 변화 0**. ⚠️**측정 오류 두 건을 스스로 잡아 초기 리포트를 정정했다**: ⓐ "브랜드 5색 중 cyan 만 쓴다"고 적었는데 **틀렸다** — 프로브가 `backgroundColor` 만 봐서 `bg-gradient-to-br from-navy via-deep-blue` 같은 **그라디언트(`background-image`)를 놓쳤다.** 재측정하니 `/login` 은 5색 중 4색을 제대로 쓴다. 실제 문제는 "시스템 미구현"이 아니라 **"랜딩만 브랜드 표면색을 안 씀"**(gold 는 전 페이지 미사용). ⓑ 터치 타깃을 **데스크톱 1280px 에서 재서** 5개라 했는데 375px 로 다시 재니 3개였다 — 나머지는 `hidden sm:flex` 데스크톱 전용이었다. 🔴**적용하지 않은 것과 이유**: **FINDING-001(faux bold)은 quick win 이 아니었다.** h1 이 `font-weight:800` 을 요구하는데 Geist 는 400/500/700 만 로드된다(`document.fonts` 실측). 파고드니 **`font-weight:700` 에 `Geist-SemiBold.woff2`(실제 600)가 매핑**돼 있었고(`index.css:16-21`), `font-extrabold` 가 **9개 파일 14곳**에 퍼져 있다. `font-bold` 로 낮추면 히어로가 눈에 띄게 가벼워지고, 제대로 고치려면 진짜 `Geist-Bold`·`Geist-Black` 재매핑이 필요한데 그건 앱 전체 `font-bold` 렌더를 바꾼다 — **디자인 시스템 결정이라 임의로 하지 않았다.** **로그인 푸터도 보류** — 구조상 가능하지만 다크 네이비 그라디언트 바로 아래 밝은 `bg-gray-100` 푸터가 붙어 라이트 모드 전환이 급하다. **검증 없이 시각 변화를 넣지 않았다.** ✅**주장만 하지 않고 빌드·프리뷰를 띄워 실측 확인했다**: 랜딩 `<footer>` `true`·문구 일치·`BridgeLogis` 1회, 375px 44px 미만 **3개 → 1개**(남은 건 로고 텍스트 링크 117×28), `/login` `h1` **0개 → 1개**. 검증: tsc 0, eslint 0, vitest **1797/1797**, build exit 0. ⚠️1802 가 아닌 건 war-risk 테스트 5건이 stash 에 있어서다. ⚠️디자인 훅이 `Header.tsx` L70·L182 와 `LandingPage.tsx` L50·L63 을 지적했으나 **전부 내가 건드리지 않은 기존 코드**임을 diff 헝크 위치(172·189·197 / 5·218)와 `git show main:` 대조로 확인했다 — 억제 규칙은 추가하지 않았다. 다만 **L50 은 진짜 발견이다**: 히어로 headline 의 `bg-clip-text text-transparent` 그라디언트 텍스트는 AI 블랙리스트 항목인데 내 감사가 놓쳤다. 리포트에 FINDING-010 으로 추가했다.
