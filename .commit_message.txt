🎨 fix(ui): 모바일 캐리어 배지 색 교정 + 존 정렬 단일화 + 자동스크롤 타이머 정리 — 미검토였던 두 파일 리뷰에서 나온 3건. ①**FedEx 배지가 데스크톱과 다른 색**: MobileLayout 이 인라인 삼항의 else 로 `bg-blue-100`(DESIGN.md 가 명시적으로 금지한 Tailwind 기본 스케일)을 쓰고 있었고, 비교 카드·DESIGN.md §8.5 는 FedEx=`cyan-*` 이다 → 같은 캐리어가 화면마다 다른 색으로 보였다. `CARRIER_BADGE_CLASS`/`CARRIER_SURFACE_CLASS` 를 carrier_metadata.ts 에 신설해 두 화면이 같은 출처를 읽게 함. **hue 토큰 조합이 아니라 완전한 리터럴 문자열로 작성** — Tailwind 는 온전한 클래스명만 스캔하므로 `bg-${hue}-100` 은 CSS 가 아예 안 나온다. 빌드 CSS 에 `bg-cyan-100`·`text-cyan-800` 방출 확인, 대비 8.61:1(AA 통과, 기존 amber 6.37:1 보다 높음. DESIGN.md 의 "cyan 텍스트 금지"는 2.46:1 인 cyan-500 얘기라 cyan-800 에는 해당 없음). 삼항 else 폴백도 제거해 캐리어 추가 시 조용히 파란 배지가 되던 것 차단 ②**존 정렬 규칙이 3벌**(sortZones + 핸들러 2곳 인라인 복제)이라 규칙 변경 시 놓치기 쉬웠다 → `zonesFor()` 로 통일, `sortZones` 는 `[...keys]` 로 비파괴 전환(호출부가 지금은 새 배열만 넘기지만 공유 배열이 오면 조용히 깨짐) ③캐리어 변경 자동스크롤 `setTimeout` 미정리 → 빠르게 전환하면 전환 횟수만큼 스크롤이 중첩. cleanup 추가. 테스트 10건 신규: 배지 3색+금지스케일, 존 정렬 3경로, 스크롤 중첩/단일/무변경. **결함주입 2종으로 게이트 증명** — numeric 정렬 제거 시 존 3건 RED(단, DHL 은 Z8 까지라 판별 불가여서 UPS 로 왕복하도록 테스트 보강), 배지는 수정 전 FedEx 2건 RED. FE 1693 green(1683→+10), tsc·lint·build clean
