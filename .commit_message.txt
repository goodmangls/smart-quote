🔌 feat(fsc): 견적 계산기가 Admin 위젯(DB) 요율을 읽도록 — 관리자가 FSC 를 올려도 견적에 반영되지 않던 구조 해소. 백엔드는 이전부터 DB 우선이었지만 계산기는 하드코딩 상수를 써서, 2026-08-24 에 DB 는 UPS 47.00% 인데 견적은 44.25% 로 나가고 있었다(총액 약 1.9% 과소 청구). 이제 `useCarrierFscDefault` 가 `useFscRates`(GET /api/v1/fsc/rates) 값을 기본값으로 쓰고 상수는 **요청 대기·실패 시 폴백**으로 남는다 — 네트워크 때문에 견적이 막히지 않고, 지난주 값으로 시작했다가 응답이 오면 스스로 교정된다. ⚠️핵심 제약 2가지를 훅이 소유한다: ①**사용자가 입력한 요율은 DB 응답이 늦게 와도 덮이지 않는다**(마지막으로 적용한 값을 ref 로 추적, 초기값도 시드해서 응답 전 입력까지 보호 — 이 레이스가 ref 를 쓴 이유다) ②캐리어를 바꾸면 새 캐리어 기본값으로 초기화(기존 동작 보존). `===` 비교라 명시적 0 도 사용자 값으로 존중한다(#82 의 0 시맨틱 유지). 빈칸 폴백도 DB 값을 쓰도록 FinancialSection 에 `carrierFscDefault` prop 을 추가하고 QuoteCalculator→InputSection/MobileLayout 으로 연결. 훅 테스트 7건 + FinancialSection 2건 신규, **결함주입 2종으로 증명** — DB 조회를 상수로 되돌리면 4건 RED, 편집 보호를 제거하면 레이스 테스트 1건 RED. ⚠️테스트 픽스처는 실제 요율(47/42.5/46.5)을 쓰지 않는다 — 상수를 그 값으로 올리는 순간 판별력이 사라진다(작성 중 실제로 겪음). CLAUDE.md/AGENTS.md 갱신: 평시 FSC 갱신은 위젯만으로 끝나고 배포 불필요, 코드 상수는 폴백·이력 시드. FE 1722 green(1713→+9), tsc·lint·build clean
