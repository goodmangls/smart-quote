⛽ UPS·DHL·FedEx FSC 2026-09-07 주간 요율 반영 (UPS 46.75% / DHL 43.5% / FedEx 46.0%) — 48.25/43.25/47.75 → 46.75/43.5/46.0. **이 커밋은 견적 금액을 바꾸지 않는다.** 적용 요율의 출처는 DB(`fsc_rates`)=Admin 위젯이고 그쪽은 이미 입력·검증을 마쳤다. 여기서 고치는 4파일은 **DB 조회 실패·요청 대기 중 폴백**이자 이력 차트 시드다(`rates.ts` · `rates.rb` · `fsc-history.ts` · `fsc-history.test.ts` — 뒤 둘을 빠뜨리기 쉬워 `fsc-apply.py` 로 묶어 처리했고 가드 테스트가 부분 갱신을 RED 로 잡는다). ✅**DB 반영을 추측이 아니라 프로덕션 응답으로 검증했다** — 계산 API 를 `marginPercent:0`·`fscPercent` 생략으로 호출해 `intlFsc/intlBase` 로 역산했고(마진 0 이어야 `base_with_margin==intl_base` 라 비율이 곧 요율이다), main·emax 둘 다 46.75/43.50/46.00 이 나왔다. ⚠️**대조군으로 명시 `fscPercent:50` 을 같이 쏴서 정확히 50.00% 가 나오는 것까지 확인**했다 — 이게 없으면 프로브가 요율이 아닌 다른 걸 재고 있어도 모른다. 갱신 전 같은 프로브가 48.25/43.25/47.75 를 반환해 **위젯 입력이 아직임을 물어보지 않고 판별**할 수 있었다. ✅DHL 은 공식 페이지가 값과 **주차까지** 확증했다(`9월 7-13, 2026 → 43.50%`; 9월 14-20 은 43.75% 로 이미 게시). UPS·FedEx 는 봇 차단이라 사용자 제공값이다. 🔴**`RouteSection.test.tsx` 실패는 이 변경과 무관한 기존 비결정 결함이다** — 처음엔 "알려진 플래키"로 넘기려 했고, stash 실행 한 번을 보고는 반대로 "내 변경이 원인"이라 단정했다. 둘 다 근거가 부족했다: 같은 트리에서 실패(2건)→실패(1건)→통과가 나왔고, **HEAD 를 반복 실행하니 2회차에서 똑같이 실패**해 기존 결함으로 확정됐다. 정적으로도 경로가 없다 — `RouteSection.tsx` 는 요율 모듈을 import 하지 않고 테스트의 `fscPercent: 30` 은 상수가 아닌 하드코딩 픽스처다. **한 번의 green/red 는 비결정 테스트에서 아무것도 증명하지 않는다.** 검증: tsc 0, eslint 0, vitest 1797(위 결함 제외), rubocop 158파일 0 offense, rspec **605/0**. smart-quote-emax 도 **같은 값·같은 날짜**로 동시 처리했고 그쪽은 472/472·327/0 전부 green. ⚠️`rates.rb` 가 바뀌므로 Render 백엔드 배포 대상이다 — **2026-09-06 에 설치한 Render GitHub App 이 push 자동배포를 실제로 발화시키는지 확인할 첫 기회**다(그동안은 `smart-quote-api/` 변경이 없어 트리거 조건 자체가 성립하지 않았다). 안 뜨면 Manual Deploy.
