🧪 test(dashboard): 위젯 테스트를 `DEFAULT_EXCHANGE_RATE` 실값에서 분리 — PR #94 의 이탈 경고 테스트 4건이 모듈 상수를 그대로 읽고 `'1,350 → 1,400'` 을 단언하고 있었다. 즉 **다음 `/fx-update` 가 값을 바꾸는 주에 이 기능과 무관하게 빨개진다**(시장이 50원 경계를 넘으면 실제로 바뀐다). `vi.hoisted` + `@/config/rates` 부분 mock 으로 적용 환율을 테스트가 소유하도록 고정했다 — `vi.mock` 이 일반 top-level const 위로 호이스팅되기 때문에 `vi.hoisted` 가 필요하다. mock 이 실제로 먹는지는 1350→1400 주입으로 확인(3건 RED, "USD 없음" 케이스는 적용값과 무관하므로 정상적으로 green 유지). 겸사겸사 픽스처 기본 환율 1385.5 를 1370 으로 낮췄다 — 1385.5 는 근접 밴드(15원) 안이라 드리프트와 무관한 테스트들까지 경고 배너를 렌더하고 있었다. 동작 변경 없음, 테스트 수 불변(1741 green). emax 포팅 PR 에서 같은 하드닝을 처음부터 적용했다.
