🔐 feat(history): 견적 이력의 마진·원가를 Admin 전용으로 제한 — **회원이 이력에서 마진율을 그대로 보고 있었다.** 이력 화면에는 역할 게이팅이 아예 없어서 `MarginText` 가 모두에게 마진율을 그렸고, 상세 모달의 `Cost Breakdown` 은 원가 스택 전체를 **Total Cost** 까지 보여줬다. ⚠️**UI 만 숨기는 방식은 쓰지 않았다** — 응답에 값이 실려 있으면 DevTools 한 번이면 읽힌다. 공유 링크 누출(`bc70ea1`)이 정확히 그 형태로 화면에는 안 보이면서 본문에는 있었다. **백엔드부터** `QuoteSerializer.summary/detail` 에 `include_margin:` 을 넣어 회원 응답에서 `profitMargin` · `marginPercent` · `totalCostAmount` · `profitAmount` · `breakdown` 을 뺐다. ⚠️`breakdown` 을 함께 빼는 이유는 그것이 `totalCost` 로 끝나기 때문이다 — `totalQuoteAmount` 와 대조하면 **마진이 산수로 역산된다.** 개별 라인(포장·운임·FSC)도 가격이 아니라 원가라 회원 몫이 아니다. ⚠️**`include_margin:` 기본값은 `false`(deny by default)** 다. 호출처가 플래그를 빠뜨리면 관리자에게 데이터가 안 보이는 쪽으로 실패한다 — 눈에 띄고 고치기 쉽다. 반대 방향은 파트너에게 넘어가고 아무도 모른다. 공유 링크 누출이 그 반대 방향이었다. 컨트롤러 4곳(`create` · `index` · `show` · `update`) 전부에 `current_user.admin?` 를 넘겼고, `create` 도 포함한 이유는 생성 응답이 `detail` 을 그대로 돌려주는 **두 번째 경로**이기 때문이다. `User#admin?` 술어 신설(역할은 admin/user/member 3종이라 admin 기준으로 판정). 🖥️ 프론트: 타입의 마진 5필드를 **optional 로 바꿔 컴파일러가 렌더 지점을 찾게 했다** — 필수로 두면 회원 응답에서 `undefined.toFixed` 로 터진다. tsc 가 짚어준 지점을 모두 게이팅: 데스크톱 테이블 **컬럼 헤더까지**(셀만 숨기면 빈 컬럼이 남는다) · 모바일 테이블(액션 우측정렬 유지) · 상세 모달 마진 카드 · **`Cost Breakdown` 섹션 전체** · `QuoteHistoryPage` 의 **평균 마진 통계 카드**(숨기지 않으면 열을 가려도 집계로 새어 나간다). 집계 자체도 `profitMargin` 이 있는 행만 대상으로 바꿨다 — 안 그러면 `undefined` 가 섞여 `NaN` 이 된다. 역할은 `QuoteCalculator` 가 이미 가진 `hideMargin` 을 내려보내 **역할 검사를 두 번 하지 않는다**(`getQuoteAccessFlags` 단일 출처). 컴포넌트 기본값도 `hideMargin = true` 로 백엔드와 같은 deny-by-default 를 유지한다. `QuoteCostBreakdown` 의 prop 은 `NonNullable` 로 좁혀, 가드를 빠뜨린 호출부가 **빈 원가표를 그리는 대신 컴파일 실패**하게 했다. 신규 테스트 17건(BE 9 + FE 8). **결함주입 5종으로 판별력 실증**: ①컨트롤러가 항상 마진 포함(원래 동작) → BE 3 RED ②serializer 기본값을 true 로 → BE 1 RED(deny-by-default 전용 케이스가 정확히 잡힘) ③회원에게 breakdown 만 남김 → BE 3 RED ④헤더 게이트 제거(빈 컬럼) → FE 2 RED ⑤컴포넌트 기본값을 노출로 → FE 2 RED, 복원 후 전건 GREEN. ⚠️BE 단언은 **응답 본문**에 한다 — 누출은 렌더가 아니라 거기 있다. 📝 기존 `QuoteHistoryTable.test.tsx` 가 마진 단언 때문에 깨졌는데, **deny-by-default 가 의도대로 동작한 것**이라 기본값을 낮추지 않고 그 케이스들이 admin 뷰임을 `hideMargin: false` 로 명시하게 고쳤다. 검증: tsc 0, eslint 0, vitest **79파일 1797건**, rubocop 158파일 0 offense, rspec **605/0**(596 → +9). ⚠️전체 vitest 동시 실행에서 `RouteSection`·`TargetMarginRulesWidget` 4건이 산발적으로 실패하는데(5~11초 소요 = 타임아웃 양상) **둘 다 이 변경이 건드리지 않은 파일이고 단독 실행 시 전건 통과**한다 — 머신 리소스 경합이다.
