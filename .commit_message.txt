🗺️ fix(ui): 어느 캐리어도 취급 못 하는 목적지에 전용 라벨 — 국가 드롭다운이 "UPS 존 없음"만 붙이던 탓에 캐리어를 바꿔보라는 뜻으로 읽혔지만, 3사 전부 존이 없는 국가에서는 그게 막다른 길이다(현재 XK/Kosovo 1건). `UNSERVICEABLE_COUNTRY_CODES` 를 3개 존 맵에서 **파생**해 신설하고(하드코딩 아님 — 존이 하나라도 추가되면 다음 빌드에 자동으로 빠짐) 해당 국가는 "전 캐리어 취급 불가"로 표기, 4개 언어 키 추가. 한 캐리어만 커버하는 국가(예: CW=DHL 전용)는 캐리어 전환이 실제 해법이므로 기존 per-carrier 라벨을 유지 — 이 구분을 테스트로 고정했다. 파생 속성은 양방향으로 단언(집합의 모든 원소가 3사 모두 미매핑 + 3사 모두 미매핑인 선택가능 국가가 전부 집합에 포함)해 하드코딩 리스트로 퇴화하면 RED. RouteSection 컴포넌트 테스트 5건 신규(테스트 자체가 없던 파일). FE 1683 tests green(1673→+10), tsc·lint clean
