🔍 fix(seo): 감사에서 나온 4건 — 랜딩 139KB 제거·라우트별 canonical·sitemap 정리·보안 헤더. 2026-08-26 `/seo audit` 결과 중 SPA 구조를 바꾸지 않고 하루 안에 되는 항목들. ①**랜딩이 PDF 청크 139KB(wire)를 받고 있었다.** 원인은 프리로드 힌트가 아니라 `manualChunks` 의 `jspdf` 강제 그룹핑이었다 — Rollup 이 Vite 의 `__vitePreload` 헬퍼를 그 청크에 넣었고, 엔트리가 모든 동적 import 때문에 그 헬퍼를 필요로 해서 `import{_ as p}from"./vendor-pdf-*.js"` 라는 **진짜 정적 의존**이 생겼다. ⚠️처음엔 `modulePreload.resolveDependencies` 로 힌트만 걸렀는데 **프리뷰 실측에서 여전히 받아왔다** — 가설이 틀렸고 검증이 잡았다. 규칙을 지우니 Rollup 이 알아서 분할(`jspdf.es.min` 391KB)하고 엔트리 의존이 사라졌다. 실측: 랜딩 네트워크에서 PDF 청크 소멸, PDF 테스트 10건 green. ②**모든 라우트의 canonical 이 홈을 가리켰다** — 785단어짜리 유일한 공개 콘텐츠 `/guide` 가 스스로 홈의 중복이라고 선언 중. `CanonicalLink` + 순수함수 `canonicalFor` 분리(react-refresh 규칙). ⚠️**JS 실행 크롤러에만 통한다** — Googlebot 은 되지만 raw HTML 크롤러는 여전히 정적값을 본다. 완전 해결은 prerender(별건). ③**sitemap 이 `/quote` 를 priority 0.9 로 광고**했는데 미인증 크롤러는 `/login` 으로 리다이렉트된다. 함께 `lastmod` 의 `new Date()` 기본값 제거 — 매 요청마다 "전 페이지 오늘 수정" 이라는 거짓 신선도 신호였다. ④`vercel.json` 에 보안 헤더 전무 → nosniff·DENY·Referrer-Policy·Permissions-Policy 추가. ⚠️**CSP 는 Report-Only 로 넣었다** — Intercom·Sentry·Vercel Analytics 의 런타임 출처를 정적으로 다 열거할 수 없어 차단형은 프로덕션을 깨뜨릴 위험이 있다. 위반 수집 후 승격할 것. 신규 테스트 12건(canonical 7·sitemap 5), 둘 다 결함주입으로 5건 RED 확인 후 복원. FE 1753 green(+12), tsc·lint·build clean.
