📄 UPS Document Tariff 교체 (신규 요율표 적용, 100셀) — ⚠️**UPS 서류 견적 원가가 5kg 이하 전 구간에서 +26.32% 오른다.** 새 표는 기존 값의 **정확히 24/19 배**이고 100셀 전부 같은 비율이라 우연이 아니라 계수 관계다(Z1 0.5kg 18,886 → 23,856 / Z9 5.0kg 190,380 → 240,480). 마진 위에 얹히므로 최종 견적이 그만큼 움직인다 — 배포 전 인지가 필요한 값이다. 100셀을 **손으로 옮기지 않고 PDF 에서 파싱해 생성**했다: 100개를 타이핑하면 고객에게 잘못된 금액이 나가는 경로가 100개 생긴다. 생성기가 존·중량 구조를 기존 표와 대조해 다르면 거부하고, `.rb` 와 `.ts` 를 같은 파싱 결과에서 함께 써서 둘이 어긋날 수 없게 했다. `tariffParity.test.ts` 주석이 정한 절차를 그대로 따랐다 — **`.rb` 수정 → `bin/rails tariff:snapshot` → `.ts` 미러링**. 스냅샷 diff 가 `ups.UPS_DOC_EXACT_RATES` **100셀로만 한정**됨을 트리 비교로 확인했다(다른 요율표 무변경). 🔴**옛 요율을 리터럴로 박아둔 테스트 2건이 이 변경을 잡았다** — FE `rateTableResolver.test.ts` 와 BE `rate_table_resolver_spec.rb` 가 JP 1kg 을 `30134`/`30_134` 로 단언하고 있었고, 새 값 `38064` 는 **PDF 의 Z2(=JP) 1.0kg = 38,064 와 일치**한다. 표가 제대로 반영됐다는 증거다. ⚠️**리터럴을 상수 참조로 "정리"하지 말 것** — 바로 아래 DHL 테스트는 `DHL_DOC_EXACT_RATES.Z1[1]` 을 참조해서 표가 틀리게 바뀌어도 그대로 통과한다. 형태를 유지하고 숫자만 갱신했으며, 왜 리터럴이어야 하는지 두 파일에 주석으로 남겼다. rubocop 11건은 생성기가 만든 후행 쉼표라 자동교정했고, **교정 후 100셀 값이 그대로임을 재파싱으로 검증**했다(쉼표만 제거). 검증: tsc 0, eslint 0, vitest **1797/1797**, rubocop 158파일 0 offense, rspec **605/0**. ⚠️**받은 PDF 의 `Customer Name` 은 `E-Max` 다**(Effective 01-Feb-26). 사용자가 smart-quote-main 대상이며 smart-quote-emax 와는 무관하다고 명시해 지시대로 적용했다. 코드베이스에서 확인 가능한 정황은 이렇다: **main 은 이미 E-Max 를 요율 출처로 쓰고 있다** — `rates.ts`/`rates.rb` 의 FedEx FSC 주석이 `주간 업데이트 (EMAX 공지)` 이고 2026-07-22(#55)부터 그렇다. 이 PDF 가 이 계정에 맞는 요율표인지는 **코드로 판별할 수 없는 사업상 사실**이라 적용 전에 확인을 받았고, ✅**사용자가 2026-09-08 에 "+26.32% 인상 맞고(UPS Document Tariff 한정), smart-quote-main 에만 적용하는 게 맞다"고 확인**했다. ⚠️조사 중 확인 — 두 저장소의 UPS **비서류** 요율은 400/400 셀이 전부 다른데 **서류** 요율은 100/100 셀이 동일했다. 도입 시점을 보면 **main 이 원본(2026-07-20, #54)이고 emax 가 2026-08-03 에 "Document Rate 포트"로 출처 없이 복사**했다 — 즉 남의 계약 요율을 쓰고 있는 쪽은 emax 다. 이번 변경으로 main 만 갱신되어 둘이 갈라지므로 **emax 의 UPS 서류 요율은 별도로 점검이 필요**하다(이번 범위 밖).
