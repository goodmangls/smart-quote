🔔 feat(dashboard): 기준환율 이탈 경고·리마인더 — 대시보드 Real-time Exchange Rates 가 원달러를 상시 보고 있는데 `DEFAULT_EXCHANGE_RATE` 는 누가 `/fx-update` 를 돌려야만 움직여서, 둘을 잇는 게 아무것도 없었다. **stale 한 기준환율이 화면상 보이지 않는 문제** — smart-quote-emax 가 5개월 뒤처져 있던 걸 2026-08-25 에야 발견한 게 정확히 이 실패 모드다. 정책(`floor(시장/50)×50`)으로 계산한 버킷이 적용값과 다르면 🔴재검토, 같은 버킷이지만 경계 15원 이내면 ⚠️근접을 위젯 상단에 띄운다. ⚠️**임계값 근거**: 위젯은 시장(중간)환율이고 정책 입력은 하나은행 송금환율이라 둘이 다르다 — 2026-08-25 실측은 1383.13 vs 1385(2원 미만)였지만 TT 스프레드가 1%(약 14원)까지 가는 경우를 감안해 15원 밴드로 잡았다(단일 측정치에 맞추지 않음). 늦게 경고하면 틀린 환율로 견적이 나가고, 일찍 경고해야 은행 페이지 한 번 보는 비용으로 끝난다. `<=` 라서 "15원 남음"도 경고하고, 버킷 바닥에 정확히 앉은 경우도 경고한다(한 틱만 내려가도 버킷이 바뀌므로). ⚠️**숫자를 번역 문자열 밖으로** 렌더한다 — 보간하면 번역이 없거나 틀릴 때 정작 실행에 필요한 숫자가 사라진다(테스트에서 실제로 드러났다). 순수 로직을 `features/dashboard/lib/fxDrift.ts` 로 분리해 테스트 15건, 위젯 4건 신규. 결함주입(drift 판정 무력화)으로 4건 RED 증명. i18n 4개 언어. FE 1741 green(1722→+19), tsc·lint·build clean
