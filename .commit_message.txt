🕸️ feat(seo): 공개 2개 라우트 빌드타임 prerender — 크롤러가 보는 본문 0자 → 1,121자/5,096자. SPA 라 모든 크롤러가 받는 HTML 이 `<div id="root"></div>` 뿐이었다. 2026-08-27 확인: 네이버 `site:bridgelogis.com` 이 **"검색결과가 없습니다"**(같은 조회에서 `bridgelogis.kr` 은 색인됨), robots.txt 가 AI 봇을 명시적으로 초대해 두었는데 정작 JS 미실행 크롤러는 아무 내용도 못 본다. ⚠️**`<App/>` 를 렌더하지 않는다** — 거기서 두 페이지는 `React.lazy` 이고 `renderToString` 은 lazy 를 해소하지 않고 `<Suspense>` **폴백(스피너)** 을 내보낸다. 그러면 유효하고 비어 있지도 않은 HTML 이 나와서 tsc·lint·vitest·build·SEO CI 가 **전부 green 인 채 스피너가 배포된다.** 그래서 두 페이지를 정적 import 하고, 빌드 스크립트가 **실제 본문 문구**(`Global Freight Networks`·`User Guide`)를 단언해 없으면 빌드를 세운다. 이 가드는 실증했다 — 페이지를 lazy 로 되돌리자 1,151바이트를 잡고 중단했고, 오류 메시지가 원인을 지목한다. ⚠️**하이드레이션 불일치는 원천적으로 없다** — 앱이 `hydrateRoot` 가 아니라 `createRoot` 라 React 가 스냅샷을 버리고 새로 렌더한다. 스냅샷은 크롤러와 첫 페인트용이다. 프로바이더 3종을 SSR 안전하게: Theme·Language 는 `localStorage` 가드, **Auth 는 `isLoading` 초기값을 `typeof window !== 'undefined'` 로** — 이게 없으면 provider 가 자기 스피너만 렌더한다(브라우저 동작 불변). ⚠️**Vercel catch-all rewrite 가 `/guide` 를 `index.html` 로 보내던 문제**를 함께 고쳤다 — 그대로 두면 `/guide` 에 **랜딩 내용**이 나가서 지금보다 나쁘다. catch-all 앞에 `/guide → /guide.html` 명시 규칙 추가. 실측 검증: JS 미실행 원문에서 `/` 1,121자·`/guide` 5,096자, 브라우저 렌더 후 단어 수는 이전과 동일(163/785), `/quote` 는 여전히 `/login` 리다이렉트(인증 게이트 무손상), 라우팅·canonical 갱신 정상. 신규 테스트 5건(산출물 가드). FE 1758 green(+5), tsc·lint·build clean. ⚠️미검증: `/guide` rewrite 는 로컬에서 재현 불가 — **프리뷰 배포에서 확인 후 머지할 것.**
