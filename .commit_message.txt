🌍 카피(마케팅) 목적지 국가 수 250+ → 220+ 로 정정, 정책 근거를 실측치로 교체 — **`250+` 는 어느 기준으로도 지지되지 않는 숫자였다.** `src/config/` 를 직접 집계: `UPS_ZONE_MAP` 203 · `DHL_ZONE_MAP` 228 · `FEDEX_ZONE_MAP` 205 → **요율 합집합 237**, 그런데 `COUNTRY_OPTIONS`(드롭다운) 는 **192** 라 방문자가 실제로 즉시 견적을 받을 수 있는 건 교집합 **191개국**이다. 즉 `250+` 는 요율 커버리지(237)를 넘고 **ISO 3166-1 전체 코드 수 249 보다도 큰 주장**이었다 — UPS·DHL·FedEx 본인들이 쓰는 공식 표현도 "220+ countries and territories" 다. 그래서 `220+` 로 통일한다: 캐리어 공식 표현과 일치하고 우리 요율 커버리지가 그보다 넓어 모순되지 않으며 분쟁 시 근거가 명확하다. ⚠️**`copy.md` 의 정책 근거 숫자 자체가 틀려 있었다** — "합산 가능 목적지 최대 약 265개국" 이라 적혀 있었으나 코드는 237 이고, 그 틀린 전제 위에 `250+` 가 세워져 있었다. 더 나쁜 건 **이 문구가 smart-quote-emax 로 복사되면서 같은 과장이 번질 뻔했다**는 점이다(emax 는 오늘 190→250 요청이 들어왔다가 실측으로 220+ 로 정정됨). 정책 문단을 실측 표로 교체하고, 숫자를 올릴 때는 표를 다시 세고 근거를 함께 적도록 명시했다. ⚠️**요율은 있는데 고를 수 없는 국가가 46개**(237−191, 대부분 소규모 속령·제재 대상국)라는 사실도 기록했다 — "1초 만에 견적 완료" 가 실제로 성립하는 범위는 191개국이며, 표기를 더 올리려면 `COUNTRY_OPTIONS` 를 먼저 넓혀야 한다. 범위: **4개 언어 전부**(`ko`·`en`·`cn`·`ja` 각 2곳 — `landing.title.*` · `landing.globalPorts`) · `LandingPage.tsx` 통계 타일 1곳 · **고객 대면 헬프센터 문서 2건**(`INTERCOM_SETUP_RUNBOOK.md` · `02-ups-vs-dhl-which-to-pick.md` — Intercom 아티클로 나가는 문구라 같은 주장을 고객에게 하고 있었다) · `copy.md`. 라벨(`landing.stat.countries`)은 4개 언어 모두 이미 숫자가 없어 손대지 않았다(타일 `value` 가 유일한 숫자 출처). ⚠️`docs/archive/2026-02/…report.md` 의 `~250+` 는 **추가된 코드 라인 수**이고 아카이브라 보존했다. 치환은 파일별 등장 횟수를 단언하는 스크립트로 수행. ⚠️소스가 아니라 **빌드 산출물로 검증**: 크롤러가 보는 **prerender 결과 `dist/index.html` 에 `220+ Countries, Instantly.`**, 번들에 4개 언어(`220+개국 국제 운임`·`220+ Countries, Instantly.`·`220+国家国际运费`·`220+ヶ国の国際運賃`) 모두 확인, `250+` 계열 0건. 검증: tsc 0, eslint 0, vitest 75파일 1758건, build+prerender 성공.
